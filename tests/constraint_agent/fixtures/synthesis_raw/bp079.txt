# 约束分类审查报告

## 修改建议汇总

需要修改的条目如下（共20条）：

---

## 1. 第11条 - 建议改为 `domain_rule`

```json
{
  "id": 11,
  "kind": "domain_rule",
  "severity": "high",
  "when": "When implementing pagination data fetching",
  "action": "add random delay between page requests in loops",
  "upgrade_reason": "该约束描述的是网站反爬虫机制的客观规律。不添加随机延迟会导致IP被封禁或请求被拒绝，这是工具/框架的硬性约束，而非社区经验。"
}
```

---

## 2. 第12条 - 建议改为 `domain_rule`

```json
{
  "id": 12,
  "kind": "domain_rule",
  "severity": "high",
  "when": "When implementing HTTP request headers",
  "action": "include User-Agent header to mimic browser request...",
  "upgrade_reason": "该约束描述的是HTTP请求的客观契约。大部分网站服务器会拒绝不含User-Agent的请求，这是工具使用的基本规则而非经验总结。"
}
```

---

## 3. 第21条 - 建议改为 `domain_rule`

```json
{
  "id": 21,
  "kind": "domain_rule",
  "severity": "medium",
  "when": "When configuring HTTP request timeouts",
  "action": "Set reasonable timeout values to prevent indefinit...",
  "upgrade_reason": "该约束描述的是网络请求的资源管理客观规律。不设置超时会导致进程无限阻塞，这是框架使用的硬性约束。"
}
```

---

## 4. 第26条 - 建议改为 `domain_rule`

```json
{
  "id": 26,
  "kind": "domain_rule",
  "severity": "high",
  "when": "When implementing functions that scrape multiple p...",
  "action": "Add random delays between page requests to avoid t...",
  "upgrade_reason": "与第11条相同，描述网站反爬虫机制的客观规律。"
}
```

---

## 5. 第38条 - 建议改为 `domain_rule`

```json
{
  "id": 38,
  "kind": "domain_rule",
  "severity": "high",
  "when": "When extracting tables from websites with unreliab...",
  "action": "implement retry logic with at least 20 retry attem...",
  "upgrade_reason": "该约束描述的是不稳定网站数据提取的客观规律。不可靠网站确实需要高重试次数，这是技术实现的基本要求而非经验之谈。"
}
```

---

## 6. 第39条 - 建议改为 `domain_rule`

```json
{
  "id": 39,
  "kind": "domain_rule",
  "severity": "high",
  "when": "When extracting HTML tables from multiple Chinese ...",
  "action": "handle requests.exceptions.Timeout explicitly in t...",
  "upgrade_reason": "该约束描述的是中文网站网络异常的客观处理规律。"
}
```

---

## 7. 第52条 - 建议改为 `domain_rule`

```json
{
  "id": 52,
  "kind": "domain_rule",
  "severity": "medium",
  "when": "When implementing retry logic for JSON requests",
  "action": "implement exponential backoff strategy for retry d...",
  "upgrade_reason": "该约束描述的是服务器资源管理的客观规律。指数退避是避免触发限流的硬性技术要求。"
}
```

---

## 8. 第54条 - 建议改为 `domain_rule`

```json
{
  "id": 54,
  "kind": "domain_rule",
  "severity": "high",
  "when": "When accessing JSON data structures returned by ma...",
  "action": "handle cases where expected nested keys may not ex...",
  "upgrade_reason": "该约束描述的是JSON数据结构的客观规律。嵌套键可能不存在是API返回数据的硬性约束。"
}
```

---

## 9. 第63条 - 建议改为 `domain_rule`

```json
{
  "id": 63,
  "kind": "domain_rule",
  "severity": "high",
  "when": "When performing type conversion on web-scraped fin...",
  "action": "preprocess columns containing percentage signs, cu...",
  "upgrade_reason": "该约束描述的是网页金融数据格式的客观规律。特殊字符处理是数据转换的基本要求。"
}
```

---

## 10. 第68条 - 建议改为 `domain_rule`

```json
{
  "id": 68,
  "kind": "domain_rule",
  "severity": "high",
  "when": "When data contains special Chinese financial symbo...",
  "action": "strip Unicode spaces and special formatting charac...",
  "upgrade_reason": "该约束描述的是中文金融数据格式的客观规律。"
}
```

---

## 11. 第126条 - 建议改为 `domain_rule`

```json
{
  "id": 126,
  "kind": "domain_rule",
  "severity": "high",
  "when": "When fetching paginated data from Chinese financia...",
  "action": "introduce random delays between 0.5-1.5 seconds be...",
  "upgrade_reason": "该约束描述的是中国金融网站反爬虫机制的客观规律。"
}
```

---

## 12. 第138条 - 建议改为 `domain_rule`

```json
{
  "id": 138,
  "kind": "domain_rule",
  "severity": "medium",
  "when": "When implementing pagination in any data-fetching ...",
  "action": "Introduce a random delay of 0.5 to 1.5 seconds bet...",
  "upgrade_reason": "该约束描述的是分页请求反爬虫的客观技术规律。"
}
```

---

## 13. 第157条 - 建议改为 `domain_rule`

```json
{
  "id": 157,
  "kind": "domain_rule",
  "severity": "high",
  "when": "When constructing stock symbols for Chinese A-shar...",
  "action": "Verify symbol format matches the target exchange (...",
  "upgrade_reason": "该约束描述的是股票代码构造的客观规则。不同交易所代码格式不同是硬性约束。"
}
```

---

## 14. 第158条 - 建议改为 `domain_rule`

```json
{
  "id": 158,
  "kind": "domain_rule",
  "severity": "high",
  "when": "When fetching stock price data for backtesting",
  "action": "Explicitly specify the price adjustment mode (qfq ...",
  "upgrade_reason": "该约束描述的是AKShare价格数据的客观契约。前复权/后复权/不复权是数据的基本属性。"
}
```

---

## 15. 第172条 - 建议改为 `domain_rule`

```json
{
  "id": 172,
  "kind": "domain_rule",
  "severity": "medium",
  "when": "When parsing JSON responses from Sina/JS APIs",
  "action": "Verify that lenient JSON parsing with demjson fall...",
  "upgrade_reason": "该约束描述的是新浪/JS API返回数据格式的客观规律。"
}
```

---

## 16. 第179条 - 建议改为 `domain_rule`

```json
{
  "id": 179,
  "kind": "domain_rule",
  "severity": "medium",
  "when": "When configuring backtesting parameters with initi...",
  "action": "Verify that the default initial_cash=1,000,000 mat...",
  "upgrade_reason": "该约束描述的是框架默认参数的客观事实。默认值是框架硬编码的属性。"
}
```

---

## 17. 第180条 - 建议改为 `domain_rule`

```json
{
  "id": 180,
  "kind": "domain_rule",
  "severity": "medium",
  "when": "When tracking main contract positions in futures b...",
  "action": "Verify that pattern matching (主力合约) correctly iden...",
  "upgrade_reason": "该约束描述的是期货主力合约识别的客观规则。"
}
```

---

## 18. 第186条 - 建议改为 `domain_rule`

```json
{
  "id": 186,
  "kind": "domain_rule",
  "severity": "medium",
  "when": "When configuring backtest commission rate for A-sh...",
  "action": "Verify that commission=0.002 matches actual broker...",
  "upgrade_reason": "该约束描述的是A股佣金的客观事实。万分之2是实际费率。"
}
```

---

## 19. 第187条 - 建议改为 `domain_rule`

```json
{
  "id": 187,
  "kind": "domain_rule",
  "severity": "high",
  "when": "When configuring PyBroker stop profit/loss paramet...",
  "action": "Verify stop_profit_pct=0.10 and stop_loss_pct=0.10...",
  "upgrade_reason": "该约束描述的是PyBroker框架参数的客观规则。"
}
```

---

## 20. 第188条 - 建议改为 `domain_rule`

```json
{
  "id": 188,
  "kind": "domain_rule",
  "severity": "medium",
  "when": "When analyzing bond price histories using bond_zh_...",
  "action": "Verify that adjustment='hfq' (backward-adjusted) i...",
  "upgrade_reason": "该约束描述的是债券价格调整模式的客观参数。"
}
```

---

## 分类统计

| 原分类 | 数量 | 建议修改为 domain_rule |
|--------|------|------------------------|
| operational_lesson | 20条 | 20条 |

**保持 claim_boundary 的条目**（34条）：这些条目确实描述的是"不能对外宣称的能力"，分类正确，无需修改。

**保持 operational_lesson 的条目**（16条）：这些条目描述的是社区经验/最佳实践，不违反客观规律，应保持原分类：
- 第10、13、28、53、64、73、88、100、111、137、153、159、160、194、205、220条