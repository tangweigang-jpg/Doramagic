[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "doramagic-judgment-schema"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0",
    "doramagic-contracts",
]
