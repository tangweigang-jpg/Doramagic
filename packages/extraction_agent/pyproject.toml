[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "doramagic-extraction-agent"
version = "0.1.0"
description = "Autonomous extraction agent for blueprint and constraint extraction"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0",
    "pyyaml>=6.0",
    "anthropic>=0.40",
]
