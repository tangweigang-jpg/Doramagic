# agent_evolution — 知识摘要

## CRITICAL (failure)
- FAILURE: Self-monitoring loops fail when the agent's self-report diverges from ground-truth performance. This happens because agents trained to be helpful will over-report confidence to avoid appearing uncertain. The failure mode is a self-monitoring...
- FAILURE: Experience replay breaks down in non-stationary agent environments because old episodes carry the reward signal from a past policy and a past world state. Replaying them naively as positive examples teaches the agent to replicate outdated behavior....
- FAILURE: Capability assessment loops produce false positives when the evaluation task set is contaminated by the agent's own training or memory data. An agent that has stored successful examples of a specific benchmark task in its episodic memory will appear...
- FAILURE: Skill acquisition fails silently when the agent's new skill overlaps with but does not replace an older conflicting skill. The agent ends up with two tools for the same semantic intent — the old one with broad but imprecise behavior and the new one...
- FAILURE: Meta-learning loops degrade when the meta-objective (learning to learn faster) is evaluated on the same distribution as the primary task objective. The agent learns to produce fast-looking learning curves on familiar task families by memorizing...
- FAILURE: Goal decomposition breaks down under resource pressure when the agent is forced to prioritize sub-goals based on urgency rather than causal dependency. The agent addresses whichever sub-goal has the nearest deadline, which frequently means...

## CONSTRAINTS
- Behavior adaptation in production agents must be gated by a rollback-safe deployment mechanism. An agent that modifies its own prompt, retrieval index, or tool routing in place — without creating a versioned snapshot first — cannot be safely rolled...
- Safety guardrails for autonomous agents must encode three non-negotiable constraints that cannot be overridden by self-improvement cycles: (1) the agent cannot expand its own permission scope without explicit human approval; (2) the agent cannot...

## PATTERNS (how to build)
- Plan-and-verify pattern for complex multi-step agent tasks reduces error cascading. Before executing a task, the agent generates an explicit execution plan listing each step, the tool to be used, expected inputs, and success criteria. The plan is...
- Checkpointing pattern for long-running agent tasks enables resumption after interruption. At each significant task milestone, serialize full agent state (task progress, collected data, next planned step, tool call results) to persistent storage....
- Uncertainty quantification pattern prevents agents from presenting speculative outputs as certain facts. When the agent is uncertain about a fact or conclusion, it should explicitly state the confidence level and the basis for uncertainty. Implement...

## RATIONALE (why it works)
- An autonomous agent cannot improve what it cannot measure. Capability assessment must happen before any self-improvement loop is started, because without a stable baseline the agent has no gradient to follow. The correct sequence is: enumerate...
- Memory in a self-improving agent must be partitioned into at least three layers — episodic (what happened), semantic (what is generally true), and procedural (how to act) — because conflating them causes catastrophic interference. When an agent...
- Feedback loops in self-evolving agents must be closed at the right temporal grain. Real-time token-level feedback (e.g., logprob signals) is too noisy for stable policy updates; task-completion feedback is too sparse to enable fast correction. The...
