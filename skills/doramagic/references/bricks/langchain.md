# langchain — 知识摘要

## CRITICAL (failure)
- FAILURE: LangChain's package split (langchain, langchain-core, langchain-community, langchain-openai, etc.) is the most common source of ImportError and version conflict in production. The UNSAID trap: langchain-core is the stable base with no third-party...
- FAILURE: The v0.1→v0.2→v0.3 migration path combined with Pydantic v2 is the single largest breakage source in LangChain production codebases. LangChain v0.2 dropped many legacy chain classes; v0.3 dropped Python 3.8 and fully migrated internal models to...
- FAILURE: LCEL streaming and callback propagation has a non-obvious gotcha: callbacks passed to .invoke() do NOT automatically propagate into nested Runnables created inside RunnableLambda or custom tools. The UNSAID mechanism: callbacks propagate through the...
- FAILURE: ConversationBufferMemory and all classes in langchain.memory are deprecated as of v0.3 and will be removed. The UNSAID problem is that they were stateful objects that mixed concerns: they stored messages AND managed the prompt injection, making them...
- FAILURE: Output parsers (PydanticOutputParser, StructuredOutputParser, JsonOutputParser) fail brittlely because they rely on the LLM following format instructions in the prompt — there is no retry or schema enforcement at the protocol level. The UNSAID fix...
- FAILURE: LangSmith tracing is enabled by setting LANGCHAIN_TRACING_V2=true and LANGCHAIN_API_KEY in environment variables, but there is no runtime error if tracing silently fails due to network issues, wrong API key, or quota exceeded — the chain still...

## CONSTRAINTS
- The 'why we removed LangChain' pattern (popularized by multiple engineering blogs in 2024) identifies over-abstraction as the core failure mode for simple use cases. LangChain's value is proportional to use-case complexity: for a single LLM call...
- Tool and function calling schemas differ materially across providers, and LangChain's tool abstraction hides this until you hit an edge case. OpenAI accepts nested object schemas; Anthropic requires all properties be present in required when strict...

## PATTERNS (how to build)
- AgentExecutor is the legacy agent runtime and is superseded by LangGraph for all non-trivial agent workflows. The UNSAID distinction: AgentExecutor is a fixed loop (reason → act → observe, repeat) with no ability to branch, retry individual steps,...
- RunnableParallel enables true fan-out execution within LCEL pipelines, running multiple branches concurrently and collecting results into a dict. The UNSAID production pattern: wrap CPU-bound branches in RunnableLambda with run_in_executor to avoid...
- Multi-turn chat applications using LangChain should use RunnableWithMessageHistory rather than manually prepending history to prompts. RunnableWithMessageHistory wraps any chain and injects message history from a configurable store...

## RATIONALE (why it works)
- LangChain's core design principle is that every component — LLMs, retrievers, output parsers, chains — implements the same Runnable interface with invoke/stream/batch/ainvoke methods. This 'everything is a Runnable' philosophy means composition is...
- LangChain Expression Language (LCEL) is the composition primitive that chains Runnables using the pipe operator (|). The UNSAID reason LCEL exists is not just syntactic sugar — it bakes in automatic streaming, parallel execution via...
- LangChain's .batch() method is the correct way to run the same chain on multiple inputs with controlled concurrency, not Python list comprehensions over .invoke(). .batch() accepts a max_concurrency parameter and uses async gather internally when...
