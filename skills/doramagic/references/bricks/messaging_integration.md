# messaging_integration — 知识摘要

## CRITICAL (failure)
- FAILURE: Do not build a multi-platform messaging integration without a unified message abstraction layer, because each platform's native message object has different schemas and the platform-specific code will spread throughout your entire codebase. Telegram...
- FAILURE: Do not implement WeChat Work (企业微信/WeCom) bot integration assuming it works like Telegram or Slack, because WeChat Work's API has fundamentally different authentication, message delivery guarantees, and content restrictions. The authentication flow...
- FAILURE: Do not implement Telegram bot polling and webhook simultaneously for the same bot token, because Telegram's architecture treats them as mutually exclusive: if a webhook URL is set, getUpdates (polling) returns an error (409 Conflict); if polling is...
- FAILURE: Do not implement bot conversation logic that assumes messages arrive in order, because all major messaging platforms have eventual delivery guarantees with no strict ordering. Telegram guarantees that messages within a single chat are delivered...
- FAILURE: Do not use Telegram Bot API long polling (getUpdates with a 60-second timeout) in an async framework (asyncio, FastAPI) without understanding that it blocks the event loop. A naive asyncio bot that calls await bot.get_updates(timeout=60) in a loop...
- FAILURE: Do not implement Slack bot posting without understanding the bot token vs. user token distinction, because they have different capabilities and different audit trail implications. Bot tokens (xoxb-) can only post as the bot identity; user tokens...

## CONSTRAINTS
- SMS/MMS messaging via Twilio, AWS SNS, or similar carriers has content constraints that differ from internet messaging: (1) a single SMS is 160 characters for ASCII or 70 characters for Unicode (any character outside ASCII causes the entire message...
- Slack workspace message retention policies configured by the workspace admin can delete messages older than N days, which affects bots that store conversation state in Slack messages or threads. A bot that relies on retrieving historical messages...

## PATTERNS (how to build)
- Telegram Bot API rate limiting is enforced at two levels that must be handled independently: per-bot global limits (30 messages/second maximum; in practice, bursting above 20/second triggers 429 errors) and per-chat limits (1 message/second per...
- Discord bot implementation must handle the Gateway intents system introduced in Discord API v8+ or the bot will silently receive no messages. Gateway intents are explicit permission flags that the bot declares at connection time, specifying which...
- Slack bot architecture must choose between Bolt framework and raw API calls based on deployment complexity, not preference. Bolt (Python, JavaScript, Java) handles Socket Mode, OAuth flow, middleware, and retry logic out of the box — for production...

## RATIONALE (why it works)
- Webhook vs polling is not a performance trade-off but an architectural philosophy choice with profound implications for reliability and operational complexity. Polling means your system initiates requests on a schedule — it is simple to implement,...
- Conversation state management is the hardest problem in multi-platform messaging integration, not API authentication or message formatting. Without explicit state management, a bot that works correctly for single-turn interactions will fail on any...
- WhatsApp Business API message templates require pre-approval by Meta before sending to customers who haven't messaged you first (outside the 24-hour customer service window). This is not a bureaucratic obstacle — it is Meta's spam prevention...
