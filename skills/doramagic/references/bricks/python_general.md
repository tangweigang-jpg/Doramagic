# python_general — 知识摘要

## CRITICAL (failure)
- FAILURE: Python module import executes top-level code immediately. Code at module scope runs on import, not on call. This means: (1) expensive initialization at module level slows startup for all importers; (2) database connections opened at module level...
- FAILURE: Python evaluates default argument values ONCE at function definition time, not on each call. def f(items=[]) creates ONE list shared across all calls — a notorious bug source. Correct pattern: def f(items=None): items = items or []. Extraction...
- FAILURE: asyncio's single-threaded event loop means ANY blocking call (time.sleep, requests.get, file I/O without aiofiles) freezes ALL concurrent tasks. This is the #1 asyncio bug: mixing sync and async code. Blocking DB drivers (psycopg2, sqlite3) block...
- FAILURE: Python's multiprocessing uses pickle to serialize objects between processes. Objects that cannot be pickled (lambda functions, nested functions, closures, file handles, database connections) cause cryptic 'cannot pickle' errors at runtime. This is...
- FAILURE: Python's threading.local() and contextvars.ContextVar are different mechanisms for thread-local / async-local state. threading.local() isolates data per OS thread. ContextVar (Python 3.7+) isolates data per asyncio Task — the correct primitive for...
- FAILURE: Python's subprocess module: subprocess.run() is the modern API (Python 3.5+); os.system() and subprocess.call() are legacy. Critical security: NEVER pass shell=True with user-controlled input — this enables shell injection equivalent to SQL...

## CONSTRAINTS
- Python's Global Interpreter Lock (GIL) prevents true parallel CPU execution in a single process. CPU-bound tasks must use multiprocessing or separate processes; threading only helps I/O-bound work. This is NOT a bug to work around per-project — it's...
- Python's match statement (structural pattern matching, Python 3.10+) is NOT the same as a switch/case in other languages. It performs STRUCTURAL matching against patterns: sequences, mappings, class instances, OR patterns, guard clauses. UNSAID:...

## PATTERNS (how to build)
- Python dependency isolation: venv (stdlib), virtualenv, conda, or uv (modern, fast). requirements.txt pins exact versions for reproducibility; pyproject.toml (PEP 517/518) is the modern standard. pip-tools compiles requirements.txt from...
- Python's logging module uses a hierarchy: root logger -> named loggers (e.g., 'myapp.db'). basicConfig() configures the root logger globally — calling it in library code pollutes all users' logging. Libraries should use logging.getLogger(__name__)...
- pyproject.toml (PEP 621) is the single source of truth for project metadata in modern Python. It replaces setup.py, setup.cfg, and requirements.txt. uv is the modern, Rust-accelerated replacement for pip + pip-tools + virtualenv. Key sections:...

## RATIONALE (why it works)
- Python uses structural/duck typing: 'if it walks like a duck and quacks like a duck, it is a duck.' Type annotations (PEP 484+) are hints, not enforcement. At runtime, any object with the right interface works. This means projects may implement...
- Python's execution model: source (.py) → bytecode (.pyc in __pycache__) → CPython VM. The bytecode is portable across platforms but version-specific. Python is interpreted but NOT line-by-line — the entire function body is compiled to bytecode...
- Python exceptions are classes; catching a base class catches all subclasses. except Exception catches almost everything (not SystemExit/KeyboardInterrupt). except BaseException catches everything including KeyboardInterrupt. UNSAID: 'except...
