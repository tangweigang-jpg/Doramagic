# django — 知识摘要

## CRITICAL (failure)
- FAILURE: The N+1 query problem: fetching 100 blog posts and then accessing post.author in a template fires 101 queries (1 for posts + 1 per post for author). Fix: Post.objects.select_related('author') for ForeignKey/OneToOne (SQL JOIN)....
- FAILURE: Django signals (pre_save, post_save, post_delete) are synchronous and fire in the same transaction. They're used for side effects: audit logging, cache invalidation, sending emails. UNSAID: signals are invisible coupling — no type checker can trace...
- FAILURE: Django wraps each request in an atomic transaction by default (ATOMIC_REQUESTS=True). UNSAID: this means a slow request holds a DB transaction open for its entire duration, blocking other queries that need row locks. For requests that do heavy...
- FAILURE: Django ORM N+1 deep patterns beyond basic select_related: (1) Reverse FK relations — Post.objects.prefetch_related('comments') prefetches all comments in one query; (2) Nested relations — prefetch_related('comments__author') chains prefetches; (3)...
- FAILURE: Django migration strategy for zero-downtime deployments: the naive approach (add non-nullable column → deploy) causes downtime because the migration adds the column with NOT NULL before the new code is deployed. Safe three-phase pattern: Phase 1 —...
- FAILURE: Django signals anti-patterns: (1) Connecting signals in models.py at module level — signals are connected every time the module is imported, leading to duplicate handler calls if the module is imported multiple times (rare but possible in test...

## CONSTRAINTS
- Django's deployment checklist (manage.py check --deploy) validates: DEBUG=False, SECRET_KEY strength, ALLOWED_HOSTS, HTTPS settings (SECURE_SSL_REDIRECT, HSTS), cookie security (SESSION_COOKIE_SECURE, CSRF_COOKIE_SECURE). UNSAID: ALLOWED_HOSTS=['*']...

## PATTERNS (how to build)
- Django migrations track schema changes as Python code. makemigrations generates them; migrate applies them. Migrations must be committed to version control — they ARE the schema history. UNSAID: squashmigrations compresses migration history for...
- Django uses a single settings.py by convention, but production projects should split settings: base.py (common), local.py (development overrides), production.py (production config). The DJANGO_SETTINGS_MODULE environment variable selects which to...
- Django REST Framework serializers are the layer between Python objects and JSON. ModelSerializer auto-generates fields from a Model. UNSAID: serializers run validation in two phases — field-level (validate_<fieldname>) then object-level...

## RATIONALE (why it works)
- Django uses MTV (Model-Template-View), NOT MVC. The Django 'View' is the controller — it handles business logic and selects templates. The 'Template' is the presentation layer. Models are Django's ORM layer with built-in validation. Key philosophy:...
- Django ORM uses an Active Record pattern — each model instance represents a DB row. QuerySets are lazy: they don't hit the DB until evaluated (iteration, slicing, len(), bool()). This means chaining .filter().exclude().order_by() doesn't query the...
- Django's security defaults: CSRF protection (CSRFMiddleware) is on by default for all POST/PUT/DELETE forms. Same-origin cookie policy prevents CSRF. XSS protection via template auto-escaping. Clickjacking protection via X-Frame-Options header....
