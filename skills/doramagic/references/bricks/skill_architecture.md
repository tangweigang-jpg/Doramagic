# skill_architecture — 知识摘要

## CRITICAL (failure)
- FAILURE: The hidden trap of ignoring the delivery channel the user is already on: when a user requests a tool via Telegram (OpenClaw), the generated tool MUST default to delivering notifications and interactions through the CURRENT Telegram channel/chat, NOT...
- FAILURE: The most common Doramagic delivery failure: assuming every user request requires generated code. When a user says 'help me practice English' or 'help me prepare for an interview', the core value is INTERACTIVE PROCESS, not an automated program....
- FAILURE: The hidden trap of over-broad allowed-tools declarations: when a skill declares permission to use every available tool, the LLM has no constraint on which tool to select and will frequently choose suboptimal paths — using a file-write tool when a...
- FAILURE: The hidden trap of missing timeout budgets per tool call: skills that invoke external APIs without per-call timeouts will block indefinitely when an upstream service hangs, consuming agent context and preventing the orchestrator from taking recovery...
- FAILURE: The hidden trap of context-length blindness in skill prompt construction: skills that concatenate external data (search results, file contents, API responses) directly into prompts will silently truncate or fail when inputs exceed the model's...
- FAILURE: The hidden trap of treating rate limit errors as permanent failures: when a skill receives HTTP 429 or an API quota error, marking the task as permanently failed and notifying the user is the most common and most damaging mistake in production...

## CONSTRAINTS
- The UNSAID constraint on tool description length and the LLM selection accuracy tradeoff: tool descriptions that are too short (under 30 words) lack sufficient context for accurate tool selection in ambiguous situations; descriptions that are too...
- The UNSAID constraint on skill versioning and backward compatibility: skills deployed to multi-user platforms like ClawHub cannot break existing users when updated. The hidden complexity: a skill's 'interface' includes not just its tool parameter...

## PATTERNS (how to build)
- The UNSAID pattern behind successful tool composition chains: high-download skills implement a 'plan-then-execute' split where a lightweight planning tool first outlines the steps needed and returns a structured plan, followed by one execution tool...
- The UNSAID pattern behind reliable multi-model skill routing: skills that need different capability levels for different subtasks should implement explicit model routing rather than using a single model for all steps. The hidden benefit: routing...
- The UNSAID pattern of the 'canary tool call' for validating skill readiness before expensive operations: before committing to a multi-step workflow that may involve irreversible actions or expensive API calls, insert a cheap, fast, read-only...

## RATIONALE (why it works)
- The WHY behind separating skill configuration from skill logic: when configuration (API keys, timeouts, model names, thresholds) is embedded in code, every environment change requires code changes and redeployment. The deeper reason is that skills...
- The WHY behind the tool-composition model over monolithic skill design: skills that do one thing well and delegate to other tools achieve dramatically higher reuse because each layer can be substituted independently. The hidden insight from GitHub...
- The WHY behind requiring structured error returns rather than raised exceptions in skill tools: agents processing tool results must handle both success and failure paths through the same parsing logic. When a tool raises an unhandled exception, the...
