# obsidian_logseq — 知识摘要

## CRITICAL (failure)
- FAILURE: Obsidian plugins extend Plugin class with onload() and onunload(). Resources registered in onload() MUST be released in onunload() to prevent memory leaks. this.registerEvent(), this.addCommand(), this.addRibbonIcon() auto-unregister on unload....
- FAILURE: Obsidian sync conflict resolution: when the same note is edited on two devices before sync, Obsidian Sync creates a conflict copy (`Note (conflict 1).md`). UNSAID: conflict copies are silent — there's no notification in the UI, only a new file in...
- FAILURE: Logseq plugin development pitfalls: Logseq's plugin SDK (`@logseq/libs`) wraps a postMessage-based IFrame communication channel — all plugin API calls are async even when they look synchronous. UNSAID: `logseq.Editor.insertBlock()` returns a Promise...
- FAILURE: Obsidian sync conflict resolution fails silently when multiple devices edit the same note simultaneously without Obsidian Sync, creating duplicate conflicted copies with arbitrary name suffixes. UNSAID: Obsidian Sync (paid) handles real-time...
- FAILURE: Obsidian community plugin updates can break vault functionality without warning because plugins have no compatibility contract with the Obsidian API versioning system. UNSAID: When Obsidian updates its core API, plugins that use deprecated or...
- FAILURE: Logseq database version (DB Logseq) is a breaking architecture change from the file-based Logseq that makes existing markdown files incompatible with the new storage model. UNSAID: Logseq's DB version (announced 2023, rolling out 2024-2025) stores...

## CONSTRAINTS
- Obsidian's wikilink resolution uses a flat namespace for note names — two notes with the same name in different folders create ambiguous links that resolve to the oldest or first-alphabetically matching note. UNSAID: Obsidian resolves ambiguous...
- Obsidian's mobile app (iOS/Android) has significant feature limitations compared to the desktop app that affect power users relying on plugin-heavy workflows. UNSAID: Many community plugins don't support mobile due to Node.js dependencies, file...

## PATTERNS (how to build)
- Obsidian plugin settings use loadData()/saveData() for persistence (stored in .obsidian/plugins/<id>/data.json). The PluginSettingTab class provides the settings UI panel. UNSAID: settings are stored as plain JSON — no schema migration built in....
- Obsidian Dataview plugin enables SQL-like queries over vault metadata. Basic TABLE query: `TABLE file.mtime, status FROM #project WHERE status != "done" SORT file.mtime DESC`. Inline queries: `= this.status` renders the current file's frontmatter...
- Obsidian Templater plugin (community) extends the built-in Templates core plugin with dynamic templates. Templater enables: `<% tp.date.now('YYYY-MM-DD') %>` for dates, `<% tp.file.title %>` for current file name, `<% await tp.system.prompt('Enter...

## RATIONALE (why it works)
- Obsidian is a local-first, Markdown-based knowledge management app. Its plugin API wraps CodeMirror 6 (editor), Electron (desktop runtime), and a custom Vault abstraction (file system). Plugins run in a Node.js-like environment with filesystem...
- Logseq is an outliner-based PKM tool with a graph database (Datascript) under the hood. Unlike Obsidian (tree of markdown files), Logseq's data model is a flat graph of blocks with parent/child relationships. Plugins use the logseq.Editor API and...
- Obsidian vs Logseq trade-off: Obsidian is file-first (Markdown files are primary, app is a viewer), Logseq is graph-first (database is primary, files are export). Obsidian's advantage: portability (open any .md file in any editor), ecosystem...
