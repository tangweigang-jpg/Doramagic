# financial_trading — 知识摘要

## CRITICAL (failure)
- FAILURE: NEVER run a trading bot on live capital without first completing a paper trading phase of at least 30 trading days. Backtests suffer from look-ahead bias (accidentally using future data), survivorship bias (only testing on assets that still exist),...
- FAILURE: Backtesting without accounting for transaction costs and slippage produces dangerously optimistic results. A strategy showing 40% annual return in a backtest may become unprofitable after realistic costs. Rule of thumb: assume 0.1% round-trip cost...
- FAILURE: NEVER hardcode take-profit and stop-loss as absolute price levels in persistent storage without also storing the entry price and R-multiple. Absolute price levels become meaningless context without the trade's reference point. The correct schema:...
- FAILURE: NEVER assume that a market order will fill at the last traded price shown. Market orders receive the next available ask price (for buys) or bid price (for sells). In illiquid markets or at market open/close, the bid-ask spread can be 0.5–2% or more....
- FAILURE: NEVER store API keys for trading accounts in plaintext config files or environment variable files checked into version control. Trading API keys with withdrawal permissions can drain an entire account within seconds if leaked. The correct approach:...
- FAILURE: Backtesting on the same data used to select strategy parameters is overfitting — the strategy is memorizing history, not discovering patterns. The correct protocol: split historical data into in-sample (use for parameter optimization) and...

## CONSTRAINTS
- US securities trading automation must comply with FINRA Pattern Day Trader (PDT) rules: accounts with less than $25,000 are limited to 3 day trades (open and close same security same day) within any 5 business days on margin accounts. Violations...
- Exchange WebSocket connections for real-time market data have keep-alive requirements that most beginner implementations miss. Binance WebSocket streams timeout after 24 hours and require reconnection, and they need a ping/pong heartbeat every 3...

## PATTERNS (how to build)
- Walk-forward optimization prevents overfitting in strategy parameter selection by validating parameter choices on out-of-sample data that was never used in optimization. The pattern: (1) divide historical data into training and validation periods...
- Portfolio construction using mean-variance optimization (Markowitz) requires robust covariance estimation that handles the curse of dimensionality problem: with N assets, the sample covariance matrix has N(N+1)/2 parameters, requiring far more...
- Statistical arbitrage pair selection requires rigorous cointegration testing rather than correlation-based pairing because high correlation does not imply mean-reverting spread behavior. Two stock prices can be highly correlated (both trending up)...

## RATIONALE (why it works)
- Yahoo Finance's free API (via yfinance) is the fastest path to historical OHLCV data for backtesting, but it is unsuitable for live trading. yfinance scrapes Yahoo's undocumented endpoints and has no SLA — it breaks silently when Yahoo changes its...
- RSI (Relative Strength Index) measures momentum, not direction. RSI > 70 signals overbought, RSI < 30 signals oversold — but these are probabilistic tendencies, not reversal guarantees. In trending markets, RSI can stay overbought for extended...
- MACD (Moving Average Convergence Divergence) is a lagging indicator — it confirms trend changes after they happen, not before. The signal line crossover (MACD line crossing above/below signal line) is the most popular entry trigger, but it generates...
