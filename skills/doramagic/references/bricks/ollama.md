# ollama — 知识摘要

## CRITICAL (failure)
- FAILURE: Ollama defaults to a context window of 2048 tokens regardless of the model's actual maximum supported context. If a conversation or prompt exceeds 2048 tokens, Ollama silently truncates from the left — the oldest messages are dropped without any...
- FAILURE: Ollama does not auto-detect and set num_ctx based on the model's advertised maximum context length. Even when pulling a model whose model card states '128K context', Ollama will still use its 2048-token default until the user explicitly sets...
- FAILURE: Ollama's concurrent request handling is controlled by OLLAMA_NUM_PARALLEL (default 1 on CPU, up to 4 on GPU). With the default of 1, all requests are serialized — a second request blocks until the first completes. This is appropriate for interactive...
- FAILURE: Ollama's GPU layer offloading (num_gpu parameter in the API or OLLAMA_NUM_GPU env var) controls how many model layers run on GPU vs CPU. Setting num_gpu=0 forces full CPU inference even on a GPU machine — this silently works but is orders of...
- FAILURE: Ollama's default listening address (0.0.0.0:11434 when OLLAMA_HOST is set to 0.0.0.0) exposes the API on all network interfaces with no authentication. Any process or device on the same network can pull models, run inference, and consume GPU...
- FAILURE: Ollama's VRAM auto-selection picks the quantization level that fits in available VRAM without explicit user configuration. However, on systems with both integrated and discrete GPUs, Ollama may incorrectly identify integrated GPU VRAM as available...

## CONSTRAINTS
- Ollama does not support fine-tuning — it is a pure inference runtime. The Modelfile ADAPTER command allows loading LoRA adapters (in GGUF format) on top of a base model, but the adapter weights must be pre-trained externally using tools like...
- Ollama's context window for a model is set at model load time and cannot be changed per-request without reloading the model. The default context length for most models in Ollama is 2048 tokens, even for models that support 128k context in their full...

## PATTERNS (how to build)
- Using Ollama with multiple models for tiered inference: configure a lightweight model (e.g., phi3-mini or qwen2.5:1.5b) for fast simple tasks and a larger model (e.g., llama3.1:70b) for complex reasoning. Ollama keeps the most recently used model in...
- Implementing a local code assistant with Ollama using code-specialized models: deploy deepseek-coder:6.7b or qwen2.5-coder:7b with a Modelfile setting PARAMETER num_ctx 16384 for sufficient code context. For IDE integration (VS Code, Neovim),...
- Using Ollama for batch document processing with rate self-limiting: implement a producer-consumer pattern where a producer enqueues document processing tasks and N consumer threads each maintain their own Ollama connection. Set N to...

## RATIONALE (why it works)
- Ollama's design premise is that running LLMs locally should be as simple as running a Docker container: one command to pull, one command to run, no API keys, no cloud dependency. The entire UX is modeled on the Docker CLI (ollama pull, ollama run,...
- The Modelfile is Ollama's primary configuration primitive: a Dockerfile-equivalent text file that specifies the base model (FROM), system prompt (SYSTEM), inference parameters (PARAMETER), and optional prompt template (TEMPLATE). Creating a custom...
- Ollama stores and runs models exclusively in GGUF format (GPT-Generated Unified Format), the successor to GGML. GGUF files encode model weights, tokenizer, and metadata in a single binary. Quantization level is the primary tradeoff between model...
