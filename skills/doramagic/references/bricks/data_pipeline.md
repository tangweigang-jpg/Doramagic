# data_pipeline — 知识摘要

## CRITICAL (failure)
- FAILURE: FAILURE MODE: Relying on created_at for incremental loading instead of updated_at (or both) causes silently missing updates. A row inserted at T=0 with created_at='2025-01-01' that is updated 90 days later will not be captured by an incremental...
- FAILURE: FAILURE MODE: Not handling late-arriving data in stream pipelines produces metrics that become incorrect retroactively as late events arrive. A pipeline that counts daily orders based on event_time will produce an initial count at T+1h that is...
- FAILURE: FAILURE MODE: Using Airflow's LocalExecutor in production for pipelines that have concurrency requirements results in resource contention and cascading failures. LocalExecutor runs tasks as subprocesses on the scheduler host — all tasks share the...
- FAILURE: FAILURE MODE: Storing pipeline secrets (database passwords, API keys, cloud credentials) in Airflow Variables or Airflow Connections without encryption at rest exposes them in the Airflow metadata database in plaintext. The default Airflow...
- FAILURE: Silent data corruption in ETL pipelines occurs when type coercion silently converts values instead of raising errors. Pandas read_csv with dtype inference will convert a column of customer IDs like 0001234 to integer 1234, silently stripping leading...
- FAILURE: Unbounded pipeline memory consumption causes OOM crashes when processing unexpectedly large partitions. A pipeline that works correctly on average-sized daily partitions fails when a spike in data volume (holiday traffic, backfill of historical...

## PATTERNS (how to build)
- Exactly-once processing semantics in distributed pipelines require idempotent operations plus deduplication at each stage boundary. True exactly-once delivery is impossible in distributed systems under certain failure modes; practical exactly-once...
- Change data capture (CDC) enables real-time replication of database changes to downstream systems without polling or application-level instrumentation. CDC reads the database transaction log (binlog in MySQL, WAL in PostgreSQL) to capture every...
- Medallion architecture organizes data lake into Bronze, Silver, and Gold layers with increasing quality and transformation at each stage. Bronze layer: raw data from all sources exactly as received, no transformation, unlimited retention,...

## RATIONALE (why it works)
- The choice between stream processing and batch processing is not primarily about latency — it is about whether the business use case requires per-event reactions or aggregate insights, and each paradigm has fundamentally different failure modes....
- Exactly-once semantics in distributed data pipelines is theoretically achievable but practically expensive — every system that claims exactly-once delivery actually implements at-least-once delivery with idempotent consumers that deduplicate on the...
- Data validation should happen at the point of ingestion — not downstream in the transformation layer — because bad data that enters a pipeline propagates multiplicatively: one invalid record in the raw layer will produce multiple corrupted derived...
