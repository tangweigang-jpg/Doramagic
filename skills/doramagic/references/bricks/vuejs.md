# vuejs — 知识摘要

## CRITICAL (failure)
- FAILURE: Destructuring a reactive() object loses reactivity. When you write `const { count } = reactive({ count: 0 })`, count is now a plain number — the Proxy binding is severed. The same trap applies to function parameters and return values. The fix is...
- FAILURE: ref() wraps primitive values in a { value } container so the Proxy can track mutations. Inside <script setup>, refs auto-unwrap in the template — you write {{ count }} not {{ count.value }}. But inside JavaScript (setup function body, composables,...
- FAILURE: Vue Router's navigation guards are often used for authentication, but the common failure is making the auth check synchronous against a reactive store value that may not be hydrated yet (e.g., after a page refresh before the user info API returns)....
- FAILURE: Vue component keys are used to force re-creation rather than reuse. The subtle failure is using a non-unique or non-stable key — such as array index — in v-for lists that undergo reordering, insertion, or deletion. When the key is the array index,...
- FAILURE: Vue's Suspense component (experimental, ships stable in Vue 3.3+) enables async component trees to show fallback UI while awaiting async setup(). The failure mode is nesting multiple async components under a single Suspense boundary and assuming...
- FAILURE: Vue 3 reactive() vs ref(): reactive() returns a Proxy of an object — destructuring it loses reactivity because you extract plain values. `const { count } = reactive({ count: 0 })` — count is now a plain number, not reactive. ref() wraps a single...

## CONSTRAINTS
- Vue 3 template directives: v-if removes/adds DOM nodes (true conditionals), v-show toggles display:none (cheap toggle for frequently-switching elements). v-if has higher toggle cost (unmount/remount), v-show has higher initial cost (always renders)....
- Vue 3 template ref limitations: `<input ref='inputRef'>` populates `inputRef` with the DOM element after mounting. Accessing `inputRef.value` before mounting (in setup(), before onMounted) returns null — the DOM doesn't exist yet. Template refs on...

## PATTERNS (how to build)
- Vue 3 script setup with defineProps and defineEmits provides the cleanest SFC authoring experience. UNSAID: defineProps automatically infers TypeScript types and generates runtime validation, so you get both DX and runtime safety. The withDefaults()...
- Vue 3 provide/inject is the recommended pattern for cross-component communication without prop drilling, and it scales better than global state for library authors. UNSAID: provide/inject creates a dependency injection tree — a parent provides a...
- Vue 3 dynamic components using <component :is=""> and defineAsyncComponent enable code-splitting at the component level. UNSAID: defineAsyncComponent(() => import('./HeavyComponent.vue')) creates a lazy-loaded component that only downloads when...

## RATIONALE (why it works)
- Vue 3's reactivity system is built on ES2015 Proxy objects rather than Object.defineProperty (Vue 2). This means Vue 3 can detect property additions, deletions, and array index mutations natively — all of which required workarounds ($set, $delete,...
- Options API organizes code by option type (data, methods, computed, watch) which co-locates Vue machinery but scatters logic for a single feature across multiple sections. Composition API organizes by logical concern: all code for a feature lives...
- The Single-File Component (.vue file) is Vue's fundamental unit of encapsulation, co-locating template, script, and scoped styles in one file. This is not a cosmetic choice: the SFC compiler is what enables scoped CSS (adding data-v-xxxx...
