# cicd_devops — 知识摘要

## CRITICAL (failure)
- FAILURE: GitHub Actions concurrency groups without cancel-in-progress: true cause silent build queue pile-ups that block PR merges for minutes. The failure pattern: a developer pushes three commits quickly; three workflow runs queue; each waits for the...
- FAILURE: Storing secrets in GitHub Actions environment variables with echo exposes them in plaintext in workflow logs. GitHub masks registered secrets but only in exact-match form — base64-encoded, URL-encoded, or chunked variants of a secret are NOT...
- FAILURE: Terraform remote state in S3 without DynamoDB locking causes state corruption when two engineers run apply simultaneously. The failure pattern is well-known but frequently skipped in bootstrap phases: two concurrent applies read the same state, both...
- FAILURE: Dependabot PRs that auto-merge without required status checks create a false sense of automated dependency hygiene. The failure pattern: Dependabot opens a PR, auto-merge fires on the 'opened' event before CI has run, the PR merges with a broken...
- FAILURE: Renovate and Dependabot both update dependencies but have fundamentally different scheduling models that create different failure modes. Dependabot opens one PR per outdated dependency immediately on schedule; Renovate batches updates into grouped...
- FAILURE: Kubernetes resource requests and limits misconfiguration is the most common cause of mysterious pod evictions and node pressure in production. The failure pattern: pods without resource requests are scheduled on any node by the Kubernetes scheduler,...

## CONSTRAINTS
- HashiCorp Vault dynamic secrets generate short-lived credentials on demand for databases, cloud providers, and PKI. The architectural constraint teams miss is that Vault leases have a TTL and must be actively renewed by the consuming application —...
- Docker image build caching requires precise cache invalidation control to prevent stale cached layers from being used when underlying dependencies have changed. The most common mistake: COPY . . (copy all source files) as an early layer causes cache...

## PATTERNS (how to build)
- Zero-downtime deployment for stateful services (databases, message queues, caches) requires a different strategy than for stateless services. Stateless services use rolling deployment with health checks. Stateful services require: (1)...
- GitOps continuous delivery uses Git as the single source of truth for both application code and infrastructure configuration, with automated reconciliation loops that apply git state to the cluster. The ArgoCD/Flux implementation: (1) infrastructure...
- Infrastructure as Code (IaC) testing with Terraform requires a multi-layer testing strategy because Terraform applies changes to real infrastructure, making traditional unit testing insufficient. The layers: (1) Static analysis — `terraform...

## RATIONALE (why it works)
- GitHub Actions workflows are declarative YAML pipelines that run on event triggers (push, pull_request, schedule, workflow_dispatch). The WHY behind the event-driven model is that it decouples when CI runs from the human workflow: a pull_request...
- Blue-green deployment maintains two identical production environments (blue = live, green = idle). Traffic switches atomically at the load balancer level when green is validated. The core WHY is risk elimination at zero downtime: because blue...
- Canary deployment routes a small percentage of traffic (typically 1–5%) to the new version before full rollout. The WHY is that production traffic has properties — distribution of user devices, geographic latency, data edge cases — that staging...
