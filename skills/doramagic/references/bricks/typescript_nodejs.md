# typescript_nodejs — 知识摘要

## CRITICAL (failure)
- FAILURE: Using 'any' defeats TypeScript's type system silently. Variables typed as 'any' spread type unsafety to every consumer: assignments from 'any' widen the receiving variable's type, function calls on 'any' return 'any', and errors that would be caught...
- FAILURE: In Node.js, an unhandled promise rejection (a rejected Promise with no .catch() or try/catch in an async context) emits an 'unhandledRejection' event. Since Node 15, this terminates the process with a non-zero exit code by default. The silent...
- FAILURE: ESM/CJS interop produces three recurring failure modes: (1) require() of an ESM-only package throws ERR_REQUIRE_ESM — fix by dynamic import() or switch to ESM; (2) Named exports from CJS re-exported through an ESM wrapper may disappear (only...
- FAILURE: Relying on ts-node or tsx directly in production is a common but dangerous shortcut. These tools transpile TypeScript on-the-fly at runtime, adding cold-start latency (200-500ms for moderate projects), preventing ahead-of-time type errors from being...
- FAILURE: Memory leaks in long-running Node.js services most commonly originate from four sources: (1) EventEmitter listeners added repeatedly without removal — always call removeListener() or use once() for one-time subscriptions, and set...
- FAILURE: TypeScript path aliases configured in tsconfig.json (compilerOptions.paths) are a compile-time feature only — tsc resolves them during type checking but does NOT rewrite import paths in emitted JavaScript. This means `import { x } from '@app/utils'`...

## CONSTRAINTS
- Node.js `libuv` thread pool (UV_THREADPOOL_SIZE, default 4) handles: `dns.lookup()` (NOT `dns.resolve*()`), `fs.*` operations, `crypto.*` synchronous operations, `zlib.*` synchronous operations. Thread pool exhaustion: if 4 DNS lookups are in...
- Node.js `fs.watch()` and `fs.watchFile()` have platform-specific behavior constraints. `fs.watch()` uses inotify (Linux), FSEvents (macOS), or ReadDirectoryChangesW (Windows) — behavior differences: macOS FSEvents may not report individual file...

## PATTERNS (how to build)
- TypeScript's `Zod` for runtime validation and type inference: `const UserSchema = z.object({ id: z.string().uuid(), email: z.string().email(), age: z.number().int().min(0).max(150) })`. `type User = z.infer<typeof UserSchema>` derives the TypeScript...
- TypeScript's `interface` vs `type` alias decision guide: interfaces can be extended with `extends`, merged (declaration merging across files), and are generally preferred for object shapes. Type aliases can represent unions, intersections, tuples,...
- Node.js HTTP/2 and HTTP/3 for performance: `http2.createServer()` or `http2.createSecureServer()` (HTTPS required for browser HTTP/2). Multiplexing: multiple requests share one TCP connection — eliminates head-of-line blocking at the HTTP layer....

## RATIONALE (why it works)
- TypeScript uses structural (duck) typing, not nominal typing. Two types are compatible if their shapes match, regardless of declared names or class hierarchies. This means a class `Cat` and a class `Dog` with identical property sets are assignable...
- Node.js runs JavaScript on a single main thread via the V8 engine and libuv event loop. I/O operations (network, filesystem) are handed off to the OS or libuv thread pool and their callbacks are queued for the event loop to process once the call...
- Node.js has two distinct module systems: CommonJS (require/module.exports, .cjs files, synchronous) and ES Modules (import/export, .mjs files or .js when package.json sets "type":"module", asynchronous static analysis). The two systems do not...
