# langgraph — 知识摘要

## CRITICAL (failure)
- FAILURE: A frequent confusion: MessageGraph is a convenience subclass of StateGraph that hardcodes state to a list of messages with an append reducer. Developers coming from simple chatbot examples then try to store arbitrary structured state (tool results,...
- FAILURE: Streaming in multi-agent LangGraph graphs has non-obvious failure modes. When a subgraph is invoked as a node, its internal token stream is swallowed by default — only the final node output surfaces to the outer graph's stream. To propagate inner...
- FAILURE: In LangGraph versions before 0.2, checkpoints were addressed by thread_id + thread_ts (a timestamp string). The 0.2.x release replaced thread_ts with checkpoint_id (a UUID). Code that passes thread_ts to get_state(), update_state(), or stream() will...
- FAILURE: LangGraph's interrupt_before and interrupt_after parameters on compile() or stream() pause execution before or after specific nodes and raise an Interrupt exception. The critical failure mode: when an interrupt fires inside a subgraph that was...
- FAILURE: LangGraph's PostgresSaver checkpointer requires the psycopg package (not psycopg2) and specifically needs connection pool support via PostgresSaver.from_conn_string(). A common failure is passing a plain psycopg connection object instead of a pool,...
- FAILURE: LangGraph's get_state() returns a StateSnapshot containing the state at the latest checkpoint for a given thread. A common failure in time-travel debugging: calling get_state(config) without specifying a checkpoint_id always returns the LATEST...

## CONSTRAINTS
- LangGraph's state serialization for checkpointing requires that all values in the state TypedDict be JSON-serializable by the checkpointer's serializer. The default serializer uses Python's pickle (for MemorySaver) or JSON (for...
- LangGraph's state schema using TypedDict requires all state fields to be JSON-serializable when using persistent checkpointers (SqliteSaver, PostgresSaver). Non-serializable objects (database connections, file handles, Python generators, LangChain...

## PATTERNS (how to build)
- LangGraph Supervisor pattern for multi-agent coordination: a supervisor agent routes tasks to specialized worker agents based on task type. Implementation: supervisor node analyzes the current task and returns a routing decision (which worker to...
- LangGraph time-travel debugging pattern: LangGraph's checkpointing enables replaying agent execution from any previous state using get_state_history(). Use case: an agent took a wrong action at step 7 of a 20-step task; use fork_from to create a new...
- LangGraph Plan-and-Execute pattern for multi-step tasks: (1) planner node generates a step-by-step plan for the user's goal as a list of tasks. (2) executor node takes the next task from the plan, executes it using tools, and updates completed_steps...

## RATIONALE (why it works)
- LangGraph models agent orchestration as an explicit directed graph where nodes are computation units and edges are conditional transitions. This deliberate choice separates control flow from computation: the graph topology encodes WHEN logic runs,...
- LangGraph's checkpointer is not a logging layer — it is the persistence primitive that makes stateful, resumable execution possible. Every graph invocation is associated with a thread_id; after each node completes the full state snapshot is...
- LangGraph state fields accept an optional reducer function (annotated via Annotated[type, reducer]) that merges a node's partial update into the existing state rather than overwriting it. The default reducer is replacement. The canonical reducer is...
