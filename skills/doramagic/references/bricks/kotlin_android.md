# kotlin_android — 知识摘要

## CRITICAL (failure)
- FAILURE: viewModelScope only cancels work it owns. The hidden failure is launching from a ViewModel into longer-lived scopes, holding UI references in collectors, or collecting hot streams forever without lifecycle-aware restart. Teams say they used...
- FAILURE: Fragments fail less because their API is bad and more because they expose two overlapping lifecycles: fragment and view. The hidden landmine is keeping references, observers, or adapters alive past onDestroyView. Most random crashes are really stale...
- FAILURE: Gradle problems on Android are usually boundary problems disguised as build errors. Plugin order, version catalogs, AGP compatibility, and convention drift compound slowly until upgrades stall. The hidden rule is to centralize build logic early;...
- FAILURE: StateFlow.collect in a Composable will re-collect from the beginning if the composable leaves and re-enters the composition, such as during navigation or configuration changes. The hidden failure is using collectAsState() on a cold or replay-1...
- FAILURE: Room's DAO @Transaction annotation ensures that multiple queries inside the annotated method run in a single transaction, but it does not protect against concurrent access from multiple coroutines or threads. The hidden failure is assuming...
- FAILURE: Android's Doze mode and App Standby buckets aggressively defer background work for apps that are not in the foreground. AlarmManager exact alarms require SCHEDULE_EXACT_ALARM permission since Android 12, and WorkManager jobs scheduled with...

## CONSTRAINTS
- Android's Strict Mode in development is not a nice-to-have—it catches main thread I/O and resource leak violations that become ANRs in production. The hidden constraint is that StrictMode violations appear as log warnings by default, not crashes, so...
- Android background work limits introduced in API 26 mean that background services cannot reliably run for more than a few minutes without using foreground services (with a persistent notification) or WorkManager. The constraint is that AlarmManager...

## PATTERNS (how to build)
- Hilt matters less as syntax sugar and more as graph ownership. It gives Android apps a repeatable way to align object lifetime with application, activity, fragment, and ViewModel scopes. The hidden win is organizational: once scope is explicit,...
- Jetpack Compose's recomposition model is additive and granular: only composables whose inputs have changed recompose. The hidden performance pattern is to pass stable, minimal data to leaf composables rather than passing entire data classes or...
- Hilt's @HiltViewModel enables ViewModels to participate in the dependency graph without requiring factory boilerplate. The hidden pattern is scope discipline: ViewModels must not hold references to Activity or Fragment context directly, only to...

## RATIONALE (why it works)
- Null safety in Kotlin is a design language, not just compiler protection. Types declare which absences are part of the domain and which are programming mistakes. The hidden payoff is social: teams argue less in comments and reviews because the API...
- Structured concurrency makes cancellation and failure propagate through a tree on purpose. The hidden philosophy is that background work should have an owner. Coroutines feel lightweight, but Kotlin wants their lifetime to be explicit so abandoned...
- Compose versus XML is a team operating model decision. Compose rewards state-driven UI, Kotlin fluency, and design iteration speed; XML still fits teams with heavy legacy views, tooling habits, or mixed libraries. The hidden mistake is migrating...
