# swift_ios — 知识摘要

## CRITICAL (failure)
- FAILURE: Retain cycles survive code review because ARC manages counts, not ownership intent. The hidden failure is long-lived closures held by views, coordinators, or tasks that also capture self strongly. weak is not the goal by itself; the real goal is to...
- FAILURE: UI updates must happen on the main thread not because Apple is arbitrary, but because UIKit and much of SwiftUI assume a serialized view state model. The hidden failure is that async code makes thread hops implicit, so background work feels safe...
- FAILURE: SwiftUI's @State and @StateObject are not the same thing even though both create view-local state. @State is for value types managed by the view; @StateObject is for reference types whose lifetime is tied to the view's identity. The hidden failure...
- FAILURE: Task cancellation in Swift structured concurrency is cooperative: the task's isCancelled flag is set, but execution continues until the code checks it or calls a cancellation-sensitive API. The hidden failure is assuming that cancelling a Task...
- FAILURE: Xcode's App Store Connect submission rejects builds that use private APIs, contain deprecated entitlements, or include frameworks with incorrect privacy manifest declarations. The hidden failure mode is that these rejections arrive after days of...
- FAILURE: SwiftUI previews break at compile time for any view that has a required environment object not provided in the preview provider. The hidden failure is that developers skip previews entirely rather than providing lightweight preview-specific...

## CONSTRAINTS
- iOS memory limits are process-scoped and not configurable. On iPhone, apps typically receive a SIGKILL after using more than 2–4 GB of memory (device-dependent), with no warning in production. The hidden constraint is that WWDC sessions and...
- SwiftUI views are value types (structs) that are recreated on every state change—expensive initialization logic in a view's body is re-executed on every rerender. The constraint is that computed properties referenced in body must be cheap to...

## PATTERNS (how to build)
- Core Data's NSFetchedResultsController is the correct bridge between a persistent store and a table or collection view. Its hidden architectural value is that it batches fetch results, pages data as the user scrolls, and delivers incremental...
- Combine's sink subscriber retains itself through the AnyCancellable token. If the token is not stored, the subscription is immediately cancelled. The correct pattern is to store tokens in a Set<AnyCancellable> on the owning object and cancel them in...
- Keychain Services is the correct storage for credentials, tokens, and sensitive user data on iOS. UserDefaults is not encrypted and is trivially readable on a jailbroken device. The hidden architecture point is access control:...

## RATIONALE (why it works)
- Protocol-oriented programming in Swift is a scalability tactic, not a purity slogan. Protocols let APIs describe capabilities without freezing inheritance trees, which matters when product code has to mix testing, previews, adapters, and platform...
- Swift prefers value types because local reasoning is cheaper than shared mutable state. Choosing struct first is really choosing copy semantics, safer concurrency, and easier previews and tests. The hidden cost of class-first design is not only...
- SwiftUI versus UIKit is a boundary decision. SwiftUI optimizes for state-driven screens and fast iteration; UIKit still wins where imperative control, mature libraries, or complex navigation edge cases dominate. The hidden mistake is mixing them...
