# multi_agent — 知识摘要

## CRITICAL (failure)
- FAILURE: Agents that call each other synchronously in a chain without depth limits will stack-overflow on recursive or cyclically-referenced task graphs. This failure is insidious because it manifests as a hard crash or a timeout rather than a logical error,...
- FAILURE: Error propagation across agent boundaries silently corrupts results when each agent translates errors into natural language before passing them upstream. A worker agent that fails will often produce a plausible-sounding fallback response...
- FAILURE: Load balancing across homogeneous worker agents fails non-obviously when workers share a mutable tool (e.g., a database write connection or a file handle) and the load balancer distributes work faster than the tool's throughput. The symptom is not...
- FAILURE: Conflict resolution between agents that produce contradictory outputs is frequently under-specified, causing the supervisor to arbitrarily pick one result or average them incorrectly. A classic case is a multi-agent research system where two agents...
- FAILURE: Deadlock in multi-agent systems occurs most commonly when two agents each hold a resource the other needs and both are waiting for the other to release. With LLM agents this is subtler than classic mutex deadlock: Agent A has produced a partial...
- FAILURE: Prompt injection attacks are uniquely dangerous in multi-agent systems because a malicious payload in one agent's input can propagate upstream through the result chain and hijack the supervisor's behavior. An attacker who controls the content of a...

## CONSTRAINTS
- The maximum practical depth for a synchronous supervisor-worker chain is 3 levels (supervisor → coordinator → worker) before latency and debugging complexity become unacceptable for production systems. Each additional level multiplies the worst-case...
- The number of tools available to any single agent must not exceed 10, and ideally should be 5 or fewer for tasks requiring reliable sequential reasoning. This is an empirical constraint derived from LLM benchmarks showing that tool-selection...

## PATTERNS (how to build)
- Production multi-agent systems require distributed tracing infrastructure that correlates all LLM calls, tool executions, and agent state transitions under a single root trace ID. Without distributed tracing, debugging a failure that involves 5...
- Human-in-the-loop interruption checkpoints should be placed at irreversible action boundaries in multi-agent pipelines, not at arbitrary intervals. Irreversible actions include: sending external communications (emails, Slack messages, API calls that...
- Cost budgeting for multi-agent pipelines requires per-pipeline token budgets enforced at the orchestration layer, not as a soft guideline. Without enforced budgets, a single runaway agent (stuck in a retry loop, performing unnecessary extended...

## RATIONALE (why it works)
- Multi-agent systems outperform single-agent systems on tasks that exceed a single context window or benefit from independent verification. The core reason is that LLMs have a fixed attention budget: when the prompt grows too long, early instructions...
- The supervisor-worker pattern (one orchestrator agent that decomposes goals and N worker agents that execute sub-tasks) is the most common and reliable multi-agent topology because it maps directly to how humans manage complex projects. The...
- Agent specialization reduces per-agent prompt size and improves task accuracy by narrowing each agent's tool set and instruction surface. A general-purpose agent carrying 15 tools will on average pick the wrong tool 2–3× more often than a specialist...
