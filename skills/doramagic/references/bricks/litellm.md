# litellm — 知识摘要

## CRITICAL (failure)
- FAILURE: AWS Bedrock, Google Vertex AI, and Azure OpenAI each have non-standard authentication flows that LiteLLM handles differently and inconsistently. Bedrock requires boto3-style credentials (AWS_ACCESS_KEY_ID + AWS_SECRET_ACCESS_KEY + AWS_REGION_NAME)...
- FAILURE: LiteLLM's cost tracking (response.usage and litellm.completion_cost()) relies on a hardcoded price table that is updated with each release but lags provider pricing changes by days to weeks. Providers that do not return token counts in their...
- FAILURE: LiteLLM's response streaming from providers that do not natively support SSE (some Bedrock models, Cohere, AI21) is emulated by the SDK: it buffers the full response, then yields a single 'chunk' that contains the entire completion. Code that relies...
- FAILURE: LiteLLM's load balancing strategy 'least-busy' routes new requests to the deployment with the fewest in-flight requests. This strategy assumes all deployments have similar throughput — when mixed-capacity deployments exist (e.g., a gpt-4o deployment...
- FAILURE: LiteLLM's exception mapping translates provider-specific errors into OpenAI-compatible exceptions (AuthenticationError, RateLimitError, ContextWindowExceededError, etc.). However, the mapping is incomplete for newer providers and model-specific...
- FAILURE: LiteLLM's automatic retry logic retries on network errors but does not retry on provider-specific quota errors (HTTP 429) by default — you must configure num_retries and enable retry_on_rate_limit=True. Without this configuration, a 429 from OpenAI...

## CONSTRAINTS
- LiteLLM's caching layer (Redis or in-memory) caches based on an exact hash of the request parameters including the full messages array, model name, and sampling parameters. Cache hits require byte-identical inputs, so any variation in message...
- LiteLLM's tool/function calling support is inconsistent across providers due to fundamental API differences. Anthropic's tool use API requires a separate tool_result message type in the conversation history that OpenAI's function calling does not...

## PATTERNS (how to build)
- LiteLLM fallback routing pattern for high availability: configure a primary provider (GPT-4 Turbo) with fallbacks to secondary (Claude 3 Sonnet) and tertiary (Gemini 1.5 Pro) providers. litellm.completion(model='gpt-4-turbo', messages=[...],...
- Observability integration with LiteLLM and Langfuse for LLM operation monitoring: set litellm.success_callback=['langfuse'] and litellm.failure_callback=['langfuse'], configure LANGFUSE_PUBLIC_KEY and LANGFUSE_SECRET_KEY environment variables. Every...
- LiteLLM text moderation pipeline: use litellm.completion() with a dedicated moderation model (openai/text-moderation-latest or a custom endpoint) before sending user input to the main LLM. Implementation: def safe_complete(messages):...

## RATIONALE (why it works)
- LiteLLM's core design principle is that every provider call should look identical to an OpenAI API call from the caller's perspective. It achieves this by translating the OpenAI request schema into each provider's native format and translating the...
- LiteLLM's router supports cost-based routing: given a list of model deployments, it can automatically select the cheapest deployment that satisfies latency and capability constraints. The underlying philosophy is that LLM calls should be...
- LiteLLM operates in two distinct modes: SDK mode (import litellm; litellm.completion()) and Proxy mode (a standalone HTTP server that exposes an OpenAI-compatible REST API). SDK mode is appropriate for single-process applications where the developer...
