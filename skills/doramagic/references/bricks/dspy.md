# dspy — 知识摘要

## CRITICAL (failure)
- FAILURE: DSPy ships with multiple optimizers (BootstrapFewShot, BootstrapFewShotWithRandomSearch, MIPRO, MIPROv2, BootstrapFineTune, etc.) and the documentation does not prominently guide which to use when, causing teams to default to BootstrapFewShot and...
- FAILURE: Every DSPy optimizer requires a development set (a list of `dspy.Example` objects with inputs and expected outputs) to evaluate candidate programs during compilation. This requirement is non-negotiable and is the most common blocker for developers...
- FAILURE: DSPy's ChainOfThought module adds a 'rationale' output field to the signature that captures the model's step-by-step reasoning before the final answer. A common failure: when the compiled program is serialized and loaded in a production environment...
- FAILURE: DSPy's optimizer compilation modifies the program in-place — after calling optimizer.compile(program, trainset=...), the original program object has been mutated with the optimized prompts and few-shot examples. If the original un-optimized program...
- FAILURE: DSPy's LM cache (enabled by default) stores all LLM calls keyed on the exact prompt text to a local SQLite file (DSPy.db). During optimizer compilation, this cache prevents re-calling the LLM for prompts seen in previous compilation runs,...
- FAILURE: DSPy's dspy.settings.configure() is a global mutable singleton that affects all DSPy operations in the process. In multi-threaded applications or when testing DSPy programs, concurrent calls to configure() or running tests in parallel can cause...

## CONSTRAINTS
- DSPy's dspy.Retrieve module requires a retriever backend (ColBERT, Weaviate, Pinecone, etc.) configured via dspy.configure(rm=retriever_model). Unlike the LM configuration, the retriever model is global state and cannot be scoped per-request with...
- DSPy's optimizer requires that the metric function is deterministic and stable: calling the metric on the same (example, prediction) pair multiple times must return the same result. LLM-based metrics using dspy.Predict for evaluation violate this...

## RATIONALE (why it works)
- DSPy's core thesis is that prompts are a compilation artifact, not a design artifact. Instead of hand-writing prompt strings, you write a program using typed modules (`dspy.Predict`, `dspy.ChainOfThought`, etc.) and then run an optimizer that...
- A DSPy Signature is a typed I/O declaration that serves as the contract between your program logic and the LLM. It specifies input fields, output fields, and an optional docstring that becomes the task instruction. Signatures can be written as...
- The metric function is the most consequential design decision in a DSPy project and is under-documented relative to its impact. A metric is a Python function `metric(example, prediction, trace=None) -> float | bool` that scores how good a program's...
