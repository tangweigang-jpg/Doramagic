# security_auth — 知识摘要

## CRITICAL (failure)
- FAILURE: The most common OAuth2 implementation failure in production is not storing the refresh token securely — developers store it in localStorage or a plain database column, allowing XSS or a DB breach to yield a persistent credential that grants access...
- FAILURE: Secret rotation that does not include updating all consumers simultaneously causes a class of outages called 'rotation brownout': the old secret is revoked, some services have fetched the new one, others are still using cached copies of the old one....
- FAILURE: Rate limiting implemented only at the application layer (counting in Redis or a counter table) fails under distributed attack because the counting itself can be bypassed by hitting multiple nodes before counters synchronize. The UNSAID failure mode:...
- FAILURE: Input sanitization that only blocks known-bad patterns (blacklist approach) reliably fails against novel encodings and context-specific injection vectors. The UNSAID failure: attackers iterate faster than blocklists are updated, and the same payload...
- FAILURE: Storing credentials in environment variables is safer than hardcoding but is not the final answer — process-level environment exposure is a common lateral movement path in containerized environments. The UNSAID failure: environment variables are...
- FAILURE: Indirect prompt injection via retrieved documents is the most underappreciated attack vector for RAG-based AI agents. The failure pattern: an attacker publishes a web page, document, or database record containing hidden LLM instructions (white text...

## CONSTRAINTS
- PKCE (Proof Key for Code Exchange) is now mandatory for all OAuth2 authorization code flows — not just public clients — because the authorization code interception attack is possible even in confidential clients when the redirect URI is a localhost...
- Dependency supply chain attacks (like the XZ Utils backdoor, event-stream incident, and polyfill.io compromise) demonstrate that any imported package is a trust boundary. The constraint for AI skill development: every new dependency is an expanded...

## PATTERNS (how to build)
- Defense in depth authentication architecture layers multiple independent security controls so that failure of any single control does not result in compromise. Layer 1 (network): firewall rules restricting authentication service access, WAF with...
- Zero-knowledge proof authentication allows users to prove knowledge of a secret without transmitting the secret itself. Relevant for applications requiring both privacy and authentication. Practical implementations: SRP (Secure Remote Password)...
- Defense against SIM-swap attacks in SMS-based MFA requires recognizing the limitations of phone-based authentication. SIM-swap fraud (social engineering a mobile carrier to transfer a victim phone number to an attacker SIM) compromises all SMS-based...

## RATIONALE (why it works)
- API keys must never be hardcoded into source code because source code — even in private repositories — is routinely leaked through accident, misconfigured access, former-employee credentials, or supply-chain attacks on dependencies. The underlying...
- The principle of least privilege (PoLP) is the single most impactful security control for AI skill and agent systems because agents operate autonomously on behalf of users and can cause irreversible damage if over-permissioned. The WHY: when an...
- Prompt injection is the defining security threat for LLM-powered agents because agents by design take actions based on text instructions, and adversaries can embed hidden instructions in any text that reaches the agent's context window — user input,...
