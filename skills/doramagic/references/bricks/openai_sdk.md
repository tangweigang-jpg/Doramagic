# openai_sdk — 知识摘要

## CRITICAL (failure)
- FAILURE: Migrating from SDK v0.x to v1.0 breaks every call site because the module-level `openai.ChatCompletion.create()` API was removed entirely — it is not deprecated with a shim, it simply does not exist. The failure mode is a hard `AttributeError` at...
- FAILURE: Token counts returned in `response.usage.prompt_tokens` and `response.usage.completion_tokens` do not always match what appears on the billing dashboard. Known causes: (1) the API counts tokens server-side using its own tokenizer version, which may...
- FAILURE: OpenAI's Assistants API maintains conversation state server-side via threads, and each thread can accumulate thousands of messages over time. The failure mode: threads are never automatically truncated or pruned — they grow indefinitely, and older...
- FAILURE: OpenAI SDK's httpx timeout configuration applies separately to connect timeout and read timeout. The default read timeout is 600 seconds, which means a hung server call will block for 10 minutes before raising a TimeoutError. For applications with...
- FAILURE: OpenAI's moderation endpoint (/v1/moderations) returns a categories dict and a category_scores dict, but the flagged boolean is computed from a proprietary threshold applied server-side to the scores. The threshold changes over time as OpenAI...
- FAILURE: The OpenAI SDK does not automatically retry on 429 (rate limit) errors unless max_retries is explicitly set at client construction time. The default max_retries value is 2, providing limited resilience for bursty traffic. Production applications...

## CONSTRAINTS
- OpenAI's o1/o3/o4 reasoning model series have significant API constraints that differ from GPT-4 class models. In the initial releases: system messages are not supported (they are merged into the first user message); temperature, top_p, and...
- The OpenAI chat completions API has a context window limit (128k tokens for GPT-4o, 200k for o-series models) that includes all messages, tool definitions, and the response. Tool definitions consume significant tokens: a typical function schema with...

## PATTERNS (how to build)
- Implementing token budget enforcement with the OpenAI SDK uses the max_tokens parameter in combination with the response usage object. The pattern: set max_tokens conservatively (e.g., 500 for most queries), inspect response.usage.completion_tokens...
- For production applications requiring prompt caching, OpenAI's prompt caching reduces costs for requests where the beginning of the prompt is identical across calls. The pattern: structure prompts so that static content (system prompt, few-shot...
- Implementing semantic similarity deduplication with OpenAI embeddings uses a batch-embed-then-cluster approach: (1) embed all texts in a single batched API call, (2) build a FAISS or sklearn NearestNeighbors index over the embeddings, (3) find all...

## RATIONALE (why it works)
- OpenAI Python SDK v1.0 (released November 2023) replaced the module-level function pattern with an explicit client instance pattern. All API calls now go through an `OpenAI()` or `AsyncOpenAI()` client object rather than...
- The SDK's streaming interface uses a context-manager pattern (`with client.chat.completions.stream(...) as stream`) for v1.x, which guarantees the underlying HTTP connection is closed even on early exit or exceptions. The older...
- The SDK provides two parallel client classes: `OpenAI` (synchronous, built on `httpx` sync transport) and `AsyncOpenAI` (async, built on `httpx` async transport). They are not interchangeable at runtime — calling `AsyncOpenAI` methods without...
