# web_browsing — 知识摘要

## CRITICAL (failure)
- FAILURE: The single most common Playwright/Puppeteer failure in production is the race between network requests and DOM readiness — specifically, calling page.waitForSelector() or reading innerHTML immediately after page.goto() before the application's...
- FAILURE: Proxy rotation as a bot evasion strategy fails when implemented as naive IP cycling without fingerprint consistency. The trap: modern anti-bot systems (Cloudflare, PerimeterX, Akamai Bot Manager) do not rely on IP alone — they correlate TLS...
- FAILURE: JavaScript rendering is not binary — it exists on a spectrum, and misclassifying a site's rendering mode causes systematic extraction failures. The failure taxonomy: (1) Server-side rendered (SSR): full HTML in the response body — plain HTTP client...
- FAILURE: BeautifulSoup's lxml parser and html.parser produce different parse trees for malformed HTML, and choosing the wrong one causes silent data loss. The failure: html.parser is Python's built-in and requires no installation, so it is the default in...
- FAILURE: Async concurrency in Python web scraping with asyncio + aiohttp produces a common failure mode: uncapped semaphore concurrency causes connection pool exhaustion or per-domain rate limiting even when the code 'looks' throttled. The trap: beginners...
- FAILURE: Storing raw HTML in a database as the scraping record is a deceptively poor archiving strategy that creates significant operational problems. The failure modes: (1) Storage cost — a single SPA page with inlined JavaScript can be 2–5 MB of raw HTML,...

## CONSTRAINTS
- robots.txt is a legal and ethical constraint that also has practical enforcement consequences. The WHY to treat it as a hard constraint rather than a suggestion: the hiQ v. LinkedIn ruling (9th Circuit, 2022) and subsequent cases have clarified that...
- Cloudflare's bot management (formerly Cloudflare Bot Fight Mode, now Turnstile + Bot Management Enterprise) operates on a fundamentally different threat model than IP-based blocking, and treating it as an IP problem leads to a cat-and-mouse loop...

## PATTERNS (how to build)
- Browser context isolation pattern for multi-tenant web browsing services: create separate browser contexts per tenant/user to prevent session bleed and data leakage. Each browser context has independent cookies, localStorage, sessionStorage, cache,...
- Request interception and response mocking pattern for reliable extraction testing: use Playwright's route interception to mock network responses in tests, enabling deterministic testing without hitting real websites. Implementation: (1) Record mode:...
- Waterfall-to-parallel extraction pipeline optimization pattern: transform sequential page extraction into parallel processing to maximize throughput. Sequential baseline: navigate to list page (2s), extract 20 URLs (0.5s), fetch each URL...

## RATIONALE (why it works)
- Headless browsers (Playwright, Puppeteer) exist because the web has fundamentally split into two rendering models: static HTML served by the server, and JavaScript-rendered HTML assembled in the client. For roughly 60% of modern web applications,...
- The API-vs-scraping decision is the single highest-leverage architectural choice in information extraction work, and it is almost always worth 30 minutes of research before writing a single line of scraping code. The reasoning: a public or...
- Rate limiting and politeness are not merely ethical — they are the primary mechanism by which scraping systems achieve long-term operational stability. Sites that detect aggressive crawling deploy IP bans, CAPTCHA walls, and honeypot traps. The WHY...
