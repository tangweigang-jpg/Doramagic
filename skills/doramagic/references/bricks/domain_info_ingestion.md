# domain_info_ingestion — 知识摘要

## CRITICAL (failure)
- FAILURE: Info ingestion pipelines need a work queue for: fetch jobs, processing jobs, and notification jobs. Celery + Redis/RabbitMQ is the classic Python task queue. ARQ (asyncio-based, simpler) is modern alternative. UNSAID: Celery's default settings have...
- FAILURE: Content fingerprinting detects near-duplicate content across different sources or URLs. Simhash (used by Google) represents a document as a 64-bit hash where similar documents have similar hashes — Hamming distance ≤ 3 indicates near-duplicates....
- FAILURE: Incremental RSS/Atom feed parsing must handle the full RFC 4287 (Atom) and RSS 2.0 spec edge cases that real-world feeds exploit. Common failure modes: (1) feeds that omit <id>/<guid> forcing URL-based deduplication; (2) feeds where <updated>...
- FAILURE: JavaScript-rendered content (SPAs, lazy-loaded elements) is invisible to HTTP-only scrapers—fetching the URL returns a nearly empty HTML skeleton, not the rendered content. Headless browser scraping (Playwright, Puppeteer) is required but comes with...
- FAILURE: Anti-bot systems (Cloudflare, Akamai Bot Manager, DataDome) detect scraping through behavioral signals beyond simple User-Agent checking: TLS fingerprint (JA3 hash), HTTP/2 settings frames, missing browser-specific headers, mouse movement patterns,...
- FAILURE: Charset detection failures corrupt multilingual content at ingestion time. HTTP Content-Type header may specify wrong charset; HTML meta charset tag overrides HTTP header per browser spec but many parsers don't implement this priority correctly;...

## CONSTRAINTS
- Info ingestion systems must respect rate limits of upstream sources: News APIs (NewsAPI, Guardian, NYT) have per-day request limits. RSS/Atom fetching should use conditional HTTP (ETag/If-Modified-Since) and back off on 429 responses. UNSAID: API...
- robots.txt is both an ethical and increasingly a legal constraint on web scraping. The Robots Exclusion Protocol specifies which paths crawlers are disallowed from accessing, and Disallow rules should be respected by any compliant bot. UNSAID:...

## PATTERNS (how to build)
- Web scraping stack: requests/httpx for HTTP, BeautifulSoup/lxml for HTML parsing, Playwright/Selenium for JavaScript-rendered pages. Scrapy is a full framework for structured scraping with pipelines, middlewares, and item processors. UNSAID:...
- Content deduplication prevents redundant storage and processing of duplicate articles. URL normalization (remove UTM params, canonicalize scheme/path) is the first pass. Content fingerprinting (SimHash, MinHash) detects near-duplicate content with...
- Data normalization transforms heterogeneous ingested content into a consistent canonical schema. Key normalization steps: Unicode normalization (NFC form), whitespace collapsing, HTML entity decoding, timezone normalization to UTC, URL...

## RATIONALE (why it works)
- Web scraping at scale requires a layered architecture separating URL discovery, fetch scheduling, content extraction, and storage. Fetcher tier handles HTTP with proper rate limiting, retry with exponential backoff, and rotating proxies. Extraction...
- HTTP conditional requests (If-Modified-Since, ETag/If-None-Match) reduce bandwidth and server load for incremental content monitoring. Server returns 304 Not Modified when content hasn't changed, avoiding retransmission. UNSAID: many CDNs and origin...
- Paywall detection classifies articles before attempting full extraction to avoid wasting resources on metered or hard-walled content. Signal-based classifier: check for presence of paywall CSS classes (paywall, subscription-required,...
