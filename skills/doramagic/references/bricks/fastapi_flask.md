# fastapi_flask — 知识摘要

## CRITICAL (failure)
- FAILURE: Flask's request, g, session, and current_app are context-local proxies, not real objects. They only work within an active request context or application context. Accessing them outside a request (e.g., in background threads, startup code, CLI...
- FAILURE: FastAPI middleware wraps the entire request/response cycle. Middleware is applied in REVERSE order of addition — last added runs first for requests, first for responses. UNSAID: adding CORSMiddleware AFTER other middleware that might return early...
- FAILURE: FastAPI's response_model parameter filters output fields and handles serialization, but it does NOT prevent sensitive fields from reaching the response if used incorrectly. Using response_model=UserInDB where UserInDB has a hashed_password field...
- FAILURE: Flask's test client (app.test_client()) handles requests synchronously and in-process. UNSAID: the test client does NOT start the actual server — it calls the WSGI application directly. This means: network-level bugs (timeouts, connection pool...
- FAILURE: Flask and FastAPI background task anti-patterns: (1) Flask's threading.Thread in a request handler — the request context is torn down when the response is sent, so accessing flask.g or current_app in the thread raises RuntimeError; correct fix is...
- FAILURE: FastAPI's Pydantic v2 validation for nested models has a breaking change from v1: nested models that reference each other (circular references) must use model_rebuild() after all models are defined. Code that defines Model A with a field of type...

## CONSTRAINTS
- Flask's session object uses a client-side signed cookie by default. Session data is stored in the cookie itself (not the server), signed with SECRET_KEY. This means: (1) session data is visible to the user (just base64-decoded), (2) session data is...
- FastAPI's dependency injection system has a scope constraint: by default, dependencies are resolved once per request and cached within that request (use_cache=True). A dependency that is marked @lru_cache at the Python level (module-level caching)...

## PATTERNS (how to build)
- Flask Blueprints organize routes into modular components, registered on the app with a URL prefix. Blueprint-specific template folders, static files, and error handlers are possible. Common pattern: api/v1/ Blueprint, auth/ Blueprint, admin/...
- FastAPI's lifespan context manager (Python 3.9+ recommended) handles startup and shutdown events. Replaces the deprecated @app.on_event('startup'). Use lifespan for: creating connection pools, loading ML models, initializing caches. UNSAID: code in...
- FastAPI JWT authentication pattern: use python-jose or PyJWT for token creation/verification; inject the current user via a Depends(get_current_user) dependency that decodes the token. The dependency returns the user object or raises...

## RATIONALE (why it works)
- FastAPI is built on Starlette (ASGI) + Pydantic (validation) + Python type hints. Its entire design philosophy is: use standard Python type annotations to drive automatic validation, serialization, and OpenAPI documentation. There is no 'magic' —...
- Flask is a WSGI micro-framework: no ORM, no auth, no form validation built in. Its philosophy is 'batteries not included' — compose your own stack. Flask uses a global application context (app_ctx) and request context (req_ctx) via thread-locals....
- WSGI vs ASGI is the fundamental divide between Flask and FastAPI deployment models. WSGI (Flask, Django) is synchronous: one request handled per worker thread/process at a time. ASGI (FastAPI, Starlette) is asynchronous: one worker can handle...
