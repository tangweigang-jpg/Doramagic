# react — 知识摘要

## CRITICAL (failure)
- FAILURE: useEffect cleanup prevents memory leaks and stale closures. The cleanup function returned from useEffect runs: (1) before the next effect, (2) on unmount. Common leaks: setInterval without clearInterval, addEventListener without removeEventListener,...
- FAILURE: React's key prop identifies elements in lists for reconciliation. Using array index as key is a common anti-pattern: when items are reordered or deleted, React reuses the wrong component instances — causing state bugs (wrong input values) and...
- FAILURE: React Server Components (RSC) and Client Components ('use client') have a hard boundary rule: Server Components CANNOT import Client Components directly — only Client Components can import other Client Components. The correct pattern is to pass...
- FAILURE: React Context is NOT a state management solution — it's a dependency injection mechanism. Context re-renders ALL consumers whenever the context value changes. Using a single large Context for global app state (user, theme, cart, notifications in one...
- FAILURE: Code splitting with React.lazy() and dynamic import() is essential for keeping initial bundle size small. Pattern: `const HeavyChart = lazy(() => import('./HeavyChart'))`, then wrap with `<Suspense fallback={...}>`. Route-level splitting is the...
- FAILURE: React synthetic events are pooled and reused in React 16 (removed in React 17+). In React 16, accessing event.target.value inside an async callback or after setState returns null — the event was recycled. Fix: extract the value before the async call...

## CONSTRAINTS
- React Hooks have two hard rules enforced by eslint-plugin-react-hooks: (1) Only call Hooks at the top level — not inside loops, conditions, or nested functions. (2) Only call Hooks from React function components or custom Hooks. UNSAID: these rules...
- React accessibility (a11y) is not optional — it's a legal requirement in many jurisdictions (ADA, WCAG 2.1 AA). Core rules: interactive elements must have accessible names (aria-label or visible text), focus management must be handled for...

## PATTERNS (how to build)
- State should live as close as possible to where it's used (state colocation). Lifting state up to the nearest common ancestor is correct when siblings need shared state. Lifting state to a global store for purely local UI state is over-engineering....
- Zustand is a minimal global state library (~1KB) that avoids React Context performance pitfalls. Unlike Context (which re-renders all consumers on every change), Zustand uses subscriptions — components only re-render when the specific slice they...
- React Suspense lets components declare loading states declaratively: wrap async-loading components in `<Suspense fallback={<Spinner/>}>`. React 18+ supports Suspense for data fetching via frameworks (Next.js, Remix) — not raw fetch calls in...

## RATIONALE (why it works)
- React's core model: UI = f(state). The UI is a pure function of state — given the same state, the same UI renders. This enables predictability and time-travel debugging. React's reconciliation diffing algorithm computes minimal DOM updates. UNSAID:...
- useReducer is preferable to useState when: (1) next state depends on previous state in complex ways, (2) multiple sub-values need updating together atomically, (3) the update logic is complex enough to benefit from testing independently. Pattern:...
- React's fiber reconciler (introduced React 16) is a complete rewrite of the original stack reconciler. Fiber introduces a linked-list-based work unit that allows rendering to be paused, resumed, and aborted. Each fiber node represents a component...
