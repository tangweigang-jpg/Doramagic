# home_assistant — 知识摘要

## CRITICAL (failure)
- FAILURE: Home Assistant performance degrades with: (1) too many template sensors recalculating on every state change, (2) automations with no trigger filter (listening to all state changes), (3) recorder logging all entities including high-frequency sensors...
- FAILURE: Home Assistant `scripts:` are reusable action sequences — functionally identical to single-purpose automations without triggers. Scripts accept variables for parameterization. UNSAID: scripts called from multiple automations with `script.turn_on`...
- FAILURE: Using coordinator-less custom integrations that poll an API every 30 seconds creates rate limit issues and inefficient update patterns. UNSAID: The correct pattern for polled integrations is DataUpdateCoordinator — a single coordinator...
- FAILURE: Placing Home Assistant on the same VLAN as guest devices or IoT devices without network segmentation creates security vulnerabilities. UNSAID: HA has access to all home systems — locks, cameras, alarms. A compromised IoT device on the same...
- FAILURE: Overloading the HA database with high-frequency sensor data causes database bloat and slow UI loading. UNSAID: A Shelly energy monitor updating every second × 24 hours × 365 days = 31.5 million database rows per sensor per year. HA's SQLite...
- FAILURE: Using WiFi-only smart devices (particularly cheap Chinese brand plugs) creates a denial-of-service risk on home WiFi networks. UNSAID: A single Tuya WiFi smart plug attempts to reconnect to WiFi every few seconds when the network is...

## CONSTRAINTS
- Home Assistant's event loop is single-threaded. ALL integration code must be async unless explicitly delegated to a thread via hass.async_add_executor_job(). Blocking calls (requests.get, time.sleep, synchronous DB) in async context freeze the...
- Home Assistant security hardening essentials: (1) Enable MFA (TOTP) for all admin accounts, (2) use a trusted reverse proxy (nginx, Caddy) for external access — never expose HA directly to the internet, (3) configure `trusted_proxies:` and...

## PATTERNS (how to build)
- Home Assistant template sensors using template: platform in configuration.yaml or via the Helper UI allow creating derived entities from existing state. UNSAID: Template sensors recalculate whenever any entity referenced in the template changes...
- Home Assistant's Zigbee2MQTT integration provides broader device support and more stable Zigbee management than the built-in ZHA (Zigbee Home Automation). UNSAID: ZHA runs inside HA and is easier to set up; Zigbee2MQTT runs as a separate process...
- Home Assistant's Energy Management dashboard tracks electricity, gas, solar, and battery storage costs and usage over time. UNSAID: Setting up Energy Management requires: electricity grid import sensor (kWh total consumed), electricity grid export...

## RATIONALE (why it works)
- Home Assistant (HA) is a Python-based home automation platform with an event-driven architecture. Core concepts: entities (represent devices/sensors), domains (groups of entities like light, switch, sensor), integrations (code that provides...
- Home Assistant's choice of Python as the primary implementation language provides access to the PyPI ecosystem but creates performance trade-offs compared to compiled alternatives. UNSAID: Python's GIL limits CPU parallelism — HA's asyncio...
- Home Assistant's local-first philosophy and rejection of mandatory cloud connectivity makes it the most resilient home automation platform for privacy-conscious users. UNSAID: Commercial smart home platforms (Google Home, Amazon Alexa, Apple HomeKit...
