# huggingface_transformers — 知识摘要

## CRITICAL (failure)
- FAILURE: `from_pretrained('meta-llama/Llama-2-70b-hf')` will silently initiate a multi-GB (often 100–140 GB for 70B models) download to `~/.cache/huggingface/hub/` on the first call with no confirmation prompt and no progress ETA shown unless the caller...
- FAILURE: Decoder-only models (GPT-2, LLaMA, Mistral, etc.) are trained with right-side content and left-side padding during batch inference, but many tokenizers default to `padding_side='right'`, which places padding tokens before the EOS position and causes...
- FAILURE: Transformers v5 (released 2024) drops first-class TensorFlow and Flax support: TF/Flax model classes, `TFAutoModel*`, and the Keras training integration are removed or moved to unmaintained legacy status, making PyTorch the sole supported framework...
- FAILURE: Instruction-tuned models require prompts formatted in their specific chat template (ChatML, Llama-2 format, Gemma format, etc.), and applying the wrong template silently degrades output quality without raising an error. As of Transformers 4.34,...
- FAILURE: PEFT's QLoRA workflow has a critical target_modules pitfall: `lora_config.target_modules` must match the exact layer name strings used in the model's named_modules(), and these differ across architectures. LLaMA-2 uses ['q_proj','v_proj']; Mistral...
- FAILURE: Adding new special tokens with `tokenizer.add_special_tokens({'additional_special_tokens': ['<TOOL>', '</TOOL>']})` resizes the tokenizer vocabulary, but the model's embedding matrix is NOT automatically resized — any forward pass after adding...

## CONSTRAINTS
- `device_map='auto'` invokes Accelerate's device dispatcher, which shards model layers across all visible devices in priority order: CUDA GPUs first, then CPU RAM, then disk offload — but the behavior diverges significantly across hardware. On a...
- fp16, bf16, and fp8 are not interchangeable precision modes: fp16 has a narrow dynamic range that causes loss spikes on LLMs unless loss scaling is active, while bf16 matches fp32's exponent range and is stable without scaling but requires Ampere+...

## PATTERNS (how to build)
- The optimal tokenization strategy for long-document tasks (RAG retrieval, document classification, summarization) is sliding window tokenization: split the document into overlapping chunks using tokenizer(text, return_overflowing_tokens=True,...
- The canonical pattern for sentence embeddings with Transformers encoder models (BERT, RoBERTa) is mean pooling over token representations weighted by attention masks, not CLS token extraction. While CLS pooling is common in classification...
- The optimal warm-up strategy for fine-tuning large pre-trained models with Transformers is linear warm-up from zero to peak learning rate over 6-10% of total training steps, combined with cosine decay or linear decay for the remainder of training....

## RATIONALE (why it works)
- The Hugging Face Hub is the gravitational center of the Transformers ecosystem: every model, tokenizer, and config is expected to live there, and `from_pretrained()` is the universal entry point that fetches, caches, and instantiates any of them...
- Transformers exposes two layers of abstraction that serve opposite audiences: `pipeline()` wraps the full pre/post-processing chain into a one-liner optimized for correctness and rapid prototyping, while the raw `AutoModel` + tokenizer API gives...
- Model weights on the Hub are increasingly distributed as `.safetensors` files rather than PyTorch `.bin` (pickle) files. Safetensors loads 2-3x faster than pickle because it supports memory-mapped, direct dtype loading with zero deserialization...
