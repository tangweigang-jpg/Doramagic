# domain_private_cloud — 知识摘要

## CRITICAL (failure)
- FAILURE: Docker containers are ephemeral — all data written inside a container is lost on container restart unless mounted to a volume or bind mount. Named volumes (managed by Docker) vs bind mounts (mapped to host directory) serve different needs. UNSAID:...
- FAILURE: Docker Compose .env files are readable by all containers in the compose project by default via ${VAR} substitution. Docker secrets (Swarm mode) provide encrypted secret storage. UNSAID: .env files committed to git repositories expose secrets — this...
- FAILURE: Kubernetes resource limits (requests vs limits) are the most misunderstood tuning knob. Requests are what the scheduler uses to place the pod; limits are the enforcement ceiling. UNSAID: setting CPU limits causes CPU throttling even when the node...
- FAILURE: Kubernetes etcd data corruption or quorum loss renders the entire cluster control plane unavailable—no new pods can be scheduled, no deployments can be updated, and kubectl commands fail. UNSAID: etcd is a Raft-based distributed key-value store...
- FAILURE: Pod security admission controller misconfiguration allows privilege escalation attacks where a compromised container can escape to the host node. Running containers with privileged:true, hostPID:true, or mounting /var/run/docker.sock gives container...
- FAILURE: Kubernetes cluster upgrade failures occur when node components (kubelet, kube-proxy) run a different version than the API server for too long. Kubernetes supports n-2 skew between kubelet and API server—a 1.28 API server supports 1.26, 1.27, and...

## CONSTRAINTS
- Self-hosted data requires a backup strategy: volume snapshots, pg_dump for PostgreSQL, SQLite backup API for SQLite, restic/rclone for file backups. 3-2-1 rule: 3 copies, 2 media, 1 offsite. UNSAID: backing up a PostgreSQL data directory while the...

## PATTERNS (how to build)
- Docker Compose services on the same network communicate by service name (not localhost). If 'web' and 'db' are services, web connects to 'db:5432', not 'localhost:5432'. UNSAID: a common mistake is hardcoding 'localhost' in app configs inside...
- Self-hosted applications typically sit behind a reverse proxy (nginx, Traefik, Caddy) that handles TLS termination, routing, and load balancing. The app receives HTTP traffic (not HTTPS) from the proxy. UNSAID: X-Forwarded-For header carries the...
- Docker health checks (HEALTHCHECK instruction or healthcheck: in compose) enable depends_on: condition: service_healthy to ensure the DB is ready before the app starts. Without healthchecks, depends_on: only waits for the container to START, not to...

## RATIONALE (why it works)
- Private cloud infrastructure vs public cloud trade-offs center on control, compliance, and total cost of ownership (TCO). Private cloud advantages: data residency compliance (GDPR, China MLPS, government classified), predictable costs at high...
- Kubernetes storage classes and persistent volume provisioning determine application data durability and performance. Storage class parameters: ReclaimPolicy (Retain vs Delete—Retain is safer for production data), VolumeBindingMode...
- Software-Defined Networking (SDN) in private cloud abstracts physical network topology from workload placement. Overlay networks (VXLAN, GENEVE) encapsulate VM/container traffic in UDP packets, allowing any VM to talk to any other regardless of...
