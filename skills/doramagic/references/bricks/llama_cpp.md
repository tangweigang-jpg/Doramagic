# llama_cpp — 知识摘要

## CRITICAL (failure)
- FAILURE: Quantization level selection involves a three-way tradeoff between size, speed, and output quality that is not linear. Q4_K_M (4-bit, K-quant, medium) is the practical sweet spot for most use cases: ~4.5 bits/weight effective, <1% perplexity...
- FAILURE: The KV cache for llama.cpp scales as O(n_layers * n_heads * context_length * 2 * element_size). For a 7B model at FP16 KV, a 4096-token context uses ~500 MB; a 32768-token context uses ~4 GB — often exceeding available VRAM and causing silent...
- FAILURE: llama.cpp's rope_freq_scale and rope_freq_base parameters must match the values the model was trained with for long-context inference to work correctly. Models trained with RoPE scaling (e.g., Llama 3.1 trained with rope_theta=500000 for 128K...
- FAILURE: llama.cpp's Python bindings (llama-cpp-python) have a separate version lifecycle from the underlying C++ library and are often 2-4 releases behind. The package ships pre-compiled wheels for common platforms, but CUDA and Metal acceleration require...
- FAILURE: llama.cpp's speculative decoding (--draft-model or --draft flag in server mode) requires the draft model to use the same tokenizer vocabulary as the target model. Unlike vLLM's stricter check, llama.cpp accepts mismatched tokenizers at load time and...
- FAILURE: llama.cpp with Metal backend on Apple Silicon fails silently when the model exceeds the unified memory budget: inference continues but all computations fall back to CPU with no warning message, resulting in 10-50x slower performance. The failure...

## CONSTRAINTS
- llama.cpp's --threads and --threads-batch flags control CPU parallelism for token generation and prompt processing respectively. The optimal --threads value is NOT the total CPU core count — it is the number of physical cores on a single NUMA node....
- llama.cpp's GGUF format encodes quantization type, model architecture, and tensor layout in a single file using a binary format with KV metadata header followed by tensor data. When loading a GGUF file, llama.cpp validates magic bytes (GGUF),...

## PATTERNS (how to build)
- Continuous batching with llama-server enables handling multiple simultaneous requests efficiently by processing tokens from different sequences in parallel within a single forward pass. Enable with --parallel N (number of simultaneous sequences) and...
- Prompt caching with llama-server's KV cache reuse is enabled via the cache_prompt parameter in /v1/completions requests. When consecutive requests share a common prefix (system prompt, few-shot examples), the server detects the matching prefix and...
- Embedding generation with llama-embedding (or llama-server /v1/embeddings) should use --pooling mean or --pooling cls depending on model training. Models trained with mean pooling (most sentence-transformers, nomic-embed-text) produce better...

## RATIONALE (why it works)
- llama.cpp was designed from first principles around CPU inference on commodity hardware, not as a GPU framework with CPU fallback. The architectural consequences permeate the codebase: memory-mapped GGUF model loading avoids copying weights into RAM...
- GGUF (GPT-Generated Unified Format) replaced GGML in August 2023 as the canonical weight format for llama.cpp. The key design decision is that GGUF is self-describing: the file header contains model architecture metadata, tokenizer vocabularies, and...
- llama.cpp supports three GPU backends with different build requirements and performance profiles. Metal (macOS/Apple Silicon) is the default on Mac and is compiled into the standard Homebrew/binary builds — no extra flags needed. CUDA requires...
