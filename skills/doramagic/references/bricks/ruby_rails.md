# ruby_rails — 知识摘要

## CRITICAL (failure)
- FAILURE: N+1 in Rails is rarely a query bug in isolation; it signals that the controller or view boundary has lost track of data shape. If the template decides what associations to touch, query planning happens too late. The durable fix is to make preload...
- FAILURE: Callbacks feel declarative until state transitions matter. The hidden failure mode is that before and after hooks spread write logic across time, so nobody can explain why a record changed without replaying the entire lifecycle. When business rules...
- FAILURE: Rails migrations are executable history, not a perfect description of current schema. The failure comes when teams write migrations that are safe only against today's models or today's data volume. Long-lived apps need reversible, data-aware, and...
- FAILURE: Concerns are for sharing a narrow role, not hiding architecture indecision. Once a model gathers cross-cutting concerns for search, billing, tags, auditing, and permissions, reading the class no longer reveals its behavior. Overusing concerns often...
- FAILURE: ActiveRecord's optimistic locking via lock_version is silently bypassed whenever code calls update_column, update_all, or direct SQL. The hidden failure is that concurrent writes look safe in code review because ActiveRecord is present, but the lock...
- FAILURE: Turbo Streams fail subtly when the server-rendered partial and the existing DOM differ in id attributes. Turbo targets elements by id, so if a model's to_param changes or IDs are generated dynamically, a replace or update action silently leaves...

## CONSTRAINTS
- Rails 7 and 8 shift defaults more than syntax. Hotwire, importmap or jsbundling, Propshaft, and authentication generators change the expected shape of a modern app. The hidden upgrade rule is to compare against new defaults first; carrying old stack...
- Kamal 2 changes the deployment mental model from capistrano-style file sync to image-based deploy. The hidden constraint is that config files and secrets must be in the image or injected via Kamal secrets at deploy time; runtime file mutations on...

## PATTERNS (how to build)
- Rails expects models to hold both persistence and a meaningful slice of business logic. The unspoken pattern is skinny controllers, rich models, and views that stay dumb enough to swap rendering strategy. Teams that force strict layered purity often...
- Use eager loading as a read-model decision, not a micro-optimization. The unstated Rails pattern is to define the object graph for a page or API response up front, then render without surprise queries. includes, preload, and eager_load are about...
- Hotwire works best when you treat HTML as the application protocol and JavaScript as an exception path. The hidden pattern is server-owned UI state, incremental page replacement, and small Stimulus islands only where browser behavior truly diverges....

## RATIONALE (why it works)
- Rails uses conventions to turn architecture into a social contract. The hidden win is not fewer config files; it is that generators, gems, and team instincts all point in the same direction. Every custom naming scheme or folder layout buys local...
- The Rails 'convention over configuration' philosophy reduces decision fatigue but creates learning overhead for developers from other frameworks. UNSAID: Rails conventions (RESTful routes, model/view/controller structure, naming conventions,...
- Ruby on Rails choosing the 'Integrated Systems' philosophy over microservices aligns with reduced complexity for the majority of applications. UNSAID: DHH's 'Majestic Monolith' essay makes the case that the operational complexity of microservices...
