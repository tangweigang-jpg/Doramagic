# domain_finance — 知识摘要

## CRITICAL (failure)
- FAILURE: NEVER use float for financial calculations. IEEE 754 floats cannot represent most decimal fractions exactly: 0.1 + 0.2 == 0.30000000000000004. This causes balance discrepancies, rounding errors in reports, and audit failures. Use Python's...
- FAILURE: NEVER implement fraud detection as a simple threshold rule on a single attribute (e.g., block if amount > $1000). Fraudsters trivially learn and evade single-threshold rules. Effective fraud detection requires multi-signal scoring: velocity checks...
- FAILURE: NEVER trust client-reported amounts for financial transactions. Amount validation must happen server-side against the authoritative price source. A common vulnerability: the client sends the price it wants to pay for an item, and the server trusts...
- FAILURE: Race conditions in financial transaction processing cause double-spending vulnerabilities when two concurrent requests both read the same balance before either write completes, allowing a user to spend more than they have. UNSAID: Classic race...
- FAILURE: Currency rounding rules vary by currency and context — using a single rounding algorithm (always round half up) for all currencies produces incorrect results for currencies with specific rounding requirements. UNSAID: ISO 4217 defines decimal places...
- FAILURE: Stripe webhook signature verification is bypassed by developers who skip it for convenience during development, creating a security vulnerability where attackers can forge payment success events. UNSAID: Stripe webhooks are sent to a public endpoint...

## CONSTRAINTS
- Financial applications require immutable audit trails — every write operation must be recorded with who, what, when, and before/after state. Soft deletes (is_deleted=True) instead of DELETE statements preserve history. UNSAID: audit logging should...
- Financial API endpoints must be idempotent: calling the same payment/transfer request twice must not double-charge. Idempotency keys (client-generated UUID per operation) are the standard pattern. The server stores idempotency_key -> result and...

## PATTERNS (how to build)
- Reconciliation is the process of verifying that two independent records of the same financial data agree. Bank reconciliation compares internal ledger records against the bank statement. Payment reconciliation matches orders → payment processor...
- Portfolio rebalancing algorithms must account for transaction costs and tax implications, not just drift from target allocation. Naive rebalancing (sell everything above target, buy everything below) maximizes turnover and tax events. Smart...
- Double-entry bookkeeping in software requires every financial transaction to be recorded as a debit and credit pair that sum to zero, ensuring the accounting equation (Assets = Liabilities + Equity) remains balanced at all times. UNSAID: Most...

## RATIONALE (why it works)
- Double-entry bookkeeping: every financial transaction debits one account and credits another by equal amounts. Assets = Liabilities + Equity is always maintained. UNSAID: most fintech projects that don't implement proper double-entry eventually...
- Financial event sourcing treats every financial state change as an immutable event in an append-only log. The current balance is derived by replaying events, not stored directly. This pattern provides: (1) complete audit trail by design; (2) ability...
- Storing financial amounts as integer cents (or smallest currency unit) rather than floating-point decimals prevents floating-point precision errors that cause incorrect financial calculations. UNSAID: JavaScript's floating-point arithmetic: 0.1 +...
