# info_aggregation — 知识摘要

## CRITICAL (failure)
- FAILURE: NewsAPI free tier silently truncates article body to 200 characters and redirects full content behind a paywall — a trap that produces apparently working pipelines that deliver useless summaries. The UNSAID failure mode: developers build and test...
- FAILURE: APScheduler's default in-memory job store causes all scheduled jobs to disappear on process restart, creating silent briefing failures in production. The UNSAID trap: in development, the process is always freshly started so scheduled jobs are always...
- FAILURE: Cron-based briefing jobs that run as the system user instead of the application user silently fail to access virtual environment dependencies and API keys stored in user-level .env files. The UNSAID failure pattern: `crontab -e` under root or cron.d...
- FAILURE: Telegram Bot API's sendMessage silently truncates messages exceeding 4096 UTF-8 characters, and the truncation point lands mid-sentence, making the briefing appear corrupted to users. The UNSAID trap: developers test with short mock summaries that...
- FAILURE: Email deliverability for automated briefings degrades to spam folders within weeks when the sending infrastructure is not properly configured, silently ending user engagement without any error logs. The UNSAID failure chain: developers use a...
- FAILURE: Rate limit errors from news APIs and LLM providers during the scheduled briefing run silently drop the entire briefing instead of partially delivering what succeeded. The UNSAID failure pattern: the briefing job fetches from 5 sources, the 3rd...

## CONSTRAINTS
- LLM context windows impose a hard upper bound on how many articles can be summarized in a single call, and this bound interacts with cost in a non-linear way. The constraint: GPT-4o has a 128K context window; at ~800 tokens per article, that allows...
- Weather API integration in daily briefings requires understanding the semantic difference between current conditions, forecasts, and alerts — and choosing the API tier accordingly. The constraint: OpenWeatherMap free tier provides current weather...

## PATTERNS (how to build)
- Incremental content freshness management uses tiered staleness policies to minimize refetch costs while maintaining acceptable freshness for different content types. Static historical content (articles from 2+ years ago) rarely changes and can be...
- Content freshness signaling provides users with explicit recency context that enables appropriate confidence calibration when consuming aggregated information. Visual freshness indicators (published X hours ago, updated Y minutes ago, breaking news...
- Graceful degradation in information aggregation serving ensures that partial system failures result in degraded but functional service rather than complete outages. The degradation hierarchy from best-to-worst: (1) full personalized results with...

## RATIONALE (why it works)
- Daily briefing systems exist because human attention is the actual scarce resource, not information. RSS, APIs, and crawlers produce raw streams; the value of an aggregation pipeline is not fetching more sources but reducing the signal-to-noise...
- Scheduling for aggregation is a product decision disguised as an infrastructure decision. The WHY: the optimal polling frequency for a news source is not determined by what the system can sustain but by the source's actual update cadence and the...
- Summarization in a recurring briefing is fundamentally different from one-shot summarization. The WHY: a daily briefing reader has already seen yesterday's context; re-explaining background every day degrades trust and increases cognitive load. The...
