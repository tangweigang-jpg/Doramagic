# crewai — 知识摘要

## CRITICAL (failure)
- FAILURE: CrewAI agents can enter infinite tool-call loops when a tool returns an error message that the LLM interprets as a signal to retry. The agent re-invokes the same tool with the same arguments indefinitely because there is no built-in retry-limit...
- FAILURE: When a task output is very long and is passed as context to the next task, CrewAI does not automatically truncate or summarize it before injection. If the combined prompt exceeds the LLM's context window, the provider silently truncates from the...
- FAILURE: CrewAI's kickoff_async() method returns an asyncio coroutine but the underlying LLM calls are made synchronously via LiteLLM's sync interface unless the agent's LLM is explicitly configured with an async client. Running multiple crews concurrently...
- FAILURE: When CrewAI agents use tools that produce very long outputs (web scraping, code execution results, file reads), the full tool output is injected into the agent's next prompt as 'Tool Output: ...'. There is no automatic truncation or summarization....
- FAILURE: CrewAI's verbose=True mode logs every agent reasoning step, tool call, and result to stdout. In production, this creates two problems: (1) log volume can reach hundreds of megabytes per crew execution for complex pipelines, overwhelming log...
- FAILURE: CrewAI tool execution silently truncates tool outputs exceeding the configured max_rpm or token budget without raising an exception. When a tool returns a large payload (e.g., a web scrape returning 50,000 characters), CrewAI may silently trim the...

## CONSTRAINTS
- CrewAI's code execution tool (CodeInterpreterTool) runs Python code in a subprocess using Docker by default for isolation, but falls back to the host Python interpreter when Docker is unavailable. The fallback is silent — no warning is raised — so...
- CrewAI's hierarchical process manager agent has a hard constraint: it cannot be the same Agent object as any worker agent. Attempting to set manager_agent= to an agent already in the agents=[] list raises a validation error at Crew instantiation....

## PATTERNS (how to build)
- For production CrewAI deployments, wrap crew.kickoff() in an async executor using asyncio.to_thread() rather than calling it directly in an async context. CrewAI's kickoff() is synchronous and blocking; calling it in an async event loop starves...
- CrewAI crews should be composed in factory functions rather than instantiated at module level to prevent LLM connection issues at import time. Module-level instantiation eagerly initializes LLM clients, which can fail if environment variables are...
- CrewAI's knowledge sources feature (KnowledgeSource, PDFKnowledgeSource, CSVKnowledgeSource) enables agents to query a vector store at reasoning time rather than having documents injected into context. Knowledge sources are attached per-agent or...

## RATIONALE (why it works)
- CrewAI models every agent as a named role with a goal and a backstory, not as a generic callable. The backstory is injected into the system prompt and shapes how the LLM frames every response. Skipping or genericizing the backstory degrades output...
- CrewAI tasks are first-class objects that carry a description, expected_output, and an assigned agent. The Crew orchestrator passes the previous task's output as context to the next task automatically in sequential mode. This means task descriptions...
- CrewAI supports two process modes: sequential (tasks run in list order, each receiving the prior output) and hierarchical (a manager agent dynamically assigns tasks to worker agents using a sub-task planning loop). Hierarchical mode uses additional...
