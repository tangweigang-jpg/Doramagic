# api_integration — 知识摘要

## CRITICAL (failure)
- FAILURE: Cursor-based pagination is significantly more robust than offset-based pagination for large or frequently-updated datasets, but the failure to use it is the most common pagination bug in production APIs. The UNSAID failure: offset pagination (LIMIT...
- FAILURE: Ignoring rate limit response headers (X-RateLimit-Remaining, X-RateLimit-Reset, Retry-After) in favor of blind exponential backoff on 429 errors wastes quota and creates unnecessary delays. The UNSAID failure: when X-RateLimit-Reset tells you...
- FAILURE: Timeout misconfiguration is the most common cause of thread pool exhaustion in API integration layers. The failure has two modes: no timeout (requests hang indefinitely, exhausting thread/connection pools) and uniform timeout (setting the same...
- FAILURE: Webhook delivery failure is almost always caused by not acknowledging receipt quickly enough, causing the sender to retry and creating duplicate deliveries. The UNSAID failure: most webhook senders (Stripe, GitHub, Slack) have delivery timeouts of...
- FAILURE: REST API versioning via URL prefix (/v1/, /v2/) creates a hidden migration cliff: when v1 is deprecated, all consumers must migrate simultaneously or lose functionality. The UNSAID failure teams discover too late: URL versioning creates N parallel...
- FAILURE: Setting timeouts based on p50 latency instead of p99 latency is a common misconfiguration that causes production timeout cascades. The failure: if an API endpoint has p50=100ms and p99=2000ms, setting timeout=200ms will cause 1% of requests to time...

## CONSTRAINTS
- Error handling in API integrations must distinguish between retryable and non-retryable errors — treating all errors the same way is a common source of both wasted retries and missed error propagation. The constraint: 5xx errors are generally...
- Webhook event delivery guarantees must be explicitly documented and matched by consumer idempotency logic. At-most-once delivery (fire and forget) is simplest but risks missed events during consumer downtime. At-least-once delivery (retry until...

## PATTERNS (how to build)
- Idempotency key pattern prevents duplicate side effects when API calls are retried due to network failures or timeouts. Problem: a payment API call times out; the client cannot know if the payment was processed; retrying risks charging the customer...
- Consumer-driven contract testing verifies API compatibility from the consumer perspective rather than just testing the provider in isolation. Traditional provider testing: provider tests that the API returns correct responses for expected inputs;...
- Bulkhead pattern isolates API integration failures to prevent resource exhaustion from spreading to unrelated functionality. Without bulkhead: service A has connection pool of 100 connections; payment API becomes slow; 100 threads pile up waiting...

## RATIONALE (why it works)
- REST and GraphQL solve different fundamental problems, and choosing the wrong one for the use case creates long-term friction that is expensive to undo. REST excels at resource-oriented APIs with predictable, cacheable access patterns — its HTTP...
- Exponential backoff with jitter is the canonical retry strategy for API calls because it simultaneously solves two problems: it avoids hammering a degraded upstream (pure exponential backoff), and it avoids synchronized retry storms where all...
- Idempotency keys are essential for any API call that causes side effects (payments, email sends, resource creation) because networks are unreliable and clients cannot know whether a request that timed out was processed or not. Without idempotency,...
