# nextjs — 知识摘要

## CRITICAL (failure)
- FAILURE: Adding 'use client' to a component marks it and all its imported subtree as Client Components, opting the entire branch out of server rendering and React Server Components benefits (zero bundle size, direct data access, no hydration cost). The...
- FAILURE: Next.js App Router caches aggressively by default at four layers: the Request Memoization cache (per-render deduplication), the Data Cache (persistent across requests, opt-out with cache:'no-store' or revalidate:0), the Full Route Cache (static HTML...
- FAILURE: Next.js App Router Server Components cannot use React hooks (useState, useEffect, useContext) or browser-only APIs (window, document, localStorage) because they execute exclusively on the server. The subtle failure mode is that adding a hook to a...
- FAILURE: Next.js middleware running on the Edge Runtime has a 1MB code size limit (compressed) and cannot import Node.js native modules (fs, crypto, net, etc.), use npm packages that depend on Node built-ins, or execute long-running synchronous work. A...
- FAILURE: Calling cookies() or headers() inside a Server Component that is exported with revalidate > 0 or from a generateStaticParams route causes a Next.js build error because static generation cannot depend on request-time values. The failure is confusing...
- FAILURE: Next.js App Router: importing a Client Component from a Server Component must be done carefully. If the Client Component uses client-only features (hooks, event handlers) and is imported without 'use client', Next.js may fail to render correctly or...

## CONSTRAINTS
- Next.js route handlers (route.ts): API routes in App Router. HTTP method handlers: `export async function GET(request, { params }) {}`, `POST`, `PUT`, `DELETE`, etc. Return `Response` or `NextResponse`. UNSAID: route handlers in App Router replace...
- Next.js generateStaticParams must return an array of param objects for dynamic routes when using static generation. UNSAID: For nested dynamic routes like [category]/[slug], generateStaticParams must be defined in the innermost segment and can use...

## PATTERNS (how to build)
- Route Handlers in Next.js App Router replace API Routes from Pages Router. They're defined in app/api/route/route.ts using exported GET, POST, PUT, DELETE functions. UNSAID: Route Handlers support the full Web API (Request/Response), enabling...
- Next.js cookies() and headers() are Server-only APIs that return read-only collections. UNSAID: These functions make a component dynamic by default because they opt out of caching (you're reading request-specific data). In Next.js 15, cookies() and...
- Next.js font optimization with next/font eliminates Cumulative Layout Shift (CLS) from web fonts. UNSAID: next/font/google downloads fonts at build time and self-hosts them — no requests to Google's servers at runtime, improving privacy and...

## RATIONALE (why it works)
- Next.js is built around a hybrid rendering philosophy: the same codebase can serve pages via Static Site Generation (SSG, built at compile time), Server-Side Rendering (SSR, rendered per request), Incremental Static Regeneration (ISR, statically...
- The App Router uses a file-system convention where the directory structure under app/ defines routes. Special filenames have fixed semantics: page.tsx renders the route UI, layout.tsx wraps a route segment and its children persistently, loading.tsx...
- Server Actions are POST endpoints automatically created by Next.js for functions marked with 'use server'. Security implications: (1) they are publicly reachable HTTP endpoints — any user can call them directly with a crafted request, so...
