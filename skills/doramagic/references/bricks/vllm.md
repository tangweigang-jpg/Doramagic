# vllm — 知识摘要

## CRITICAL (failure)
- FAILURE: vLLM defaults gpu_memory_utilization=0.9, meaning it pre-reserves 90% of GPU VRAM for the KV-cache page pool at startup. Under concurrent high-context requests this headroom is consumed entirely, producing CUDA out-of-memory errors that crash the...
- FAILURE: vLLM treats AWQ, GPTQ, FP8, and GGUF as distinct quantization backends with separate kernel implementations — they are NOT interchangeable. Common failure modes: (1) Loading an AWQ-quantized model without `--quantization awq` causes silent weight...
- FAILURE: vLLM's speculative decoding (--speculative-model) requires that the draft model and target model share identical vocabulary and tokenizer. Using a mismatched draft model (e.g., a 1B distilled model with a different tokenizer than the 70B target)...
- FAILURE: vLLM's --max-model-len parameter truncates input prompts silently by default when they exceed the configured limit. A request with a 16K-token prompt sent to a server configured with --max-model-len 8192 will be truncated to 8192 tokens from the...
- FAILURE: vLLM multi-node inference using Ray (--worker-use-ray with multiple hosts) has a common deployment failure where workers start successfully but inference silently hangs. The root cause is almost always network asymmetry: Ray's collective...
- FAILURE: vLLM's quantized model loading with AWQ fails silently when the CUDA kernel for the specific quantization method is not compiled for the current CUDA compute capability. AWQ CUDA kernels are compiled for specific SM versions (SM75, SM80, SM86, SM89,...

## CONSTRAINTS
- vLLM tensor parallelism (--tensor-parallel-size N) requires all N GPUs to have identical VRAM capacity and ideally be connected via NVLink or NVSwitch. Critical gotchas: (1) Mixed GPU types in one tensor-parallel group (e.g., A100-40GB + A100-80GB)...
- Many models on HuggingFace Hub require `--trust-remote-code` to load custom tokenizers, attention implementations, or architecture files that are not part of vLLM's built-in model registry. Enabling this flag executes arbitrary Python code...

## PATTERNS (how to build)
- vLLM's OpenAI-compatible server (vllm.entrypoints.openai.api_server) can serve multiple models simultaneously using the --model argument list and model-specific routing. The deployment pattern for multi-model serving: start a separate vLLM server...
- vLLM's speculative decoding (--speculative-model, --num-speculative-tokens) accelerates generation by having a small draft model generate N candidate tokens, then verifying all N with a single forward pass of the large target model. The...
- vLLM's Python async client (AsyncLLMEngine) provides non-blocking inference for production applications built with asyncio. The correct assembly: (1) initialize AsyncLLMEngine once at application startup with the model configuration; (2) submit...

## RATIONALE (why it works)
- vLLM exists because traditional LLM inference pre-allocates contiguous KV-cache memory for the maximum sequence length, wasting 60-80% of GPU memory on internal and external fragmentation. PagedAttention solves this by managing KV cache in...
- vLLM's prefix caching feature (enable via --enable-prefix-caching) reuses KV cache pages for identical prompt prefixes across requests. When multiple requests share a long system prompt or RAG preamble, the prefix pages are computed once and then...
- vLLM exposes Prometheus metrics at /metrics when --enable-prometheus is set. The four metrics that matter most for SLO monitoring are: vllm:time_to_first_token_seconds (TTFT histogram), vllm:time_per_output_token_seconds (TPOT histogram for...
