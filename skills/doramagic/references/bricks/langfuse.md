# langfuse — 知识摘要

## CRITICAL (failure)
- FAILURE: Langfuse's Python SDK sends trace data asynchronously in a background thread using an internal queue. When a Python process exits normally (script ends, serverless function returns, pytest session ends), the background thread is a daemon thread and...
- FAILURE: Langfuse calculates generation cost by multiplying reported token counts by a price table it maintains internally. This produces incorrect cost figures in two common scenarios: (1) when using models whose pricing Langfuse has not yet added or...
- FAILURE: Langfuse's OpenAI integration (from langfuse.openai import openai) monkey-patches the OpenAI client to auto-instrument all calls. This is convenient but creates a subtle conflict: if any other library in the process also patches the OpenAI client...
- FAILURE: Langfuse's trace IDs are client-generated UUIDs that must be globally unique. A common failure in multi-worker deployments: if the trace ID is generated from a non-random seed (e.g., hashing the user_id + timestamp with low-resolution timestamps),...
- FAILURE: Langfuse's ClickHouse-based analytics (in self-hosted v2+) runs aggregation queries asynchronously — dashboard charts may show stale data for 1-5 minutes after traces are ingested, depending on ClickHouse materialized view refresh intervals. Teams...
- FAILURE: Langfuse's @observe() decorator does not capture exceptions by default—if a decorated function raises an exception, the trace is marked as errored but the exception details (type, message, traceback) are not automatically captured. Teams discover...

## CONSTRAINTS
- Langfuse's SDK enforces a maximum payload size of 1MB per ingestion event by default. Traces where a single LLM call's input or output exceeds 1MB (common with vision inputs, large document processing, or very long conversations) will fail silently...
- Langfuse's metadata field has a size limit of 16KB per field when stored in PostgreSQL's JSONB column. Traces with large metadata (e.g., logging full document content, large retrieval results, or extensive configuration objects) will be silently...

## PATTERNS (how to build)
- Langfuse dataset management best practice: create datasets that reflect your production distribution, not just edge cases or hard examples. A common anti-pattern is building datasets entirely from 'interesting failures'—these create a biased...
- Langfuse multi-model experiment comparison pattern: use datasets and experiments to objectively compare model providers or versions. Workflow: (1) create or reuse a representative dataset with expected outputs; (2) write an experiment runner that...
- Langfuse cost optimization strategy: use token-level tracking to identify prompt verbosity waste. Many production LLM applications have prompt templates with boilerplate that was added during development and never revisited. Langfuse's token...

## RATIONALE (why it works)
- Langfuse organizes observability data in a three-level hierarchy: Trace → Span → Generation. A Trace is the top-level container for one logical user interaction (e.g., a single chat turn or pipeline run). Spans are arbitrary nested units of work...
- Langfuse treats prompts as first-class versioned artifacts stored server-side, not as strings in application code. Prompts are fetched at runtime via `langfuse.get_prompt('prompt-name')` and linked to generations so each trace records exactly which...
- Langfuse offers two Python instrumentation styles: the `@observe()` decorator and the low-level `trace/span/generation` context manager API. The decorator is appropriate for instrumenting entire functions where the function boundary equals the span...
