# email_automation — 知识摘要

## CRITICAL (failure)
- FAILURE: Do not use keyword-based spam detection as the primary spam filter for an email automation system, because it fails against modern spam in both directions: it produces high false positives on legitimate transactional email (order confirmations,...
- FAILURE: Do not implement follow-up tracking by polling the sent folder for replies, because this approach silently fails in three common scenarios: (1) the recipient replies from a different email address than the one you sent to; (2) the reply is forwarded...
- FAILURE: Do not use SMTP for sending bulk automated emails (newsletters, campaign emails, batch notifications) through a standard business email account because: (1) ISPs apply strict sending limits — Gmail allows 500 emails/day for consumer, 2,000/day for...
- FAILURE: Do not rely on 'Delivered-To' header or the SMTP RCPT TO address to determine which of the user's email addresses received a message, because Gmail silently rewrites these for alias addresses. If a user has aliases (e.g., user@company.com and...
- FAILURE: Do not build email classification models by fine-tuning on a single user's inbox without stratified sampling across time, because email distributions shift dramatically over months and years (a startup's email in year 1 is mostly fundraising; in...
- FAILURE: Do not store email content in plaintext in the automation system's database, because email inherently contains sensitive personal data (PII, financial information, health data, legal communications) that is subject to GDPR, HIPAA, and other...

## CONSTRAINTS
- Gmail API batch requests are limited to 100 requests per batch call, and the Gmail API has a per-user quota of 250 quota units per second with individual operations costing between 1 and 100 units depending on the operation. Email automation agents...
- Email HTML rendering varies significantly across clients: Outlook 2016–2021 uses Microsoft Word's rendering engine (not a browser), which does not support CSS Flexbox, Grid, or many CSS3 properties. Gmail strips style tags from embedded CSS and...

## PATTERNS (how to build)
- Auto-reply drafting for email agents requires a two-stage architecture: intent classification followed by template-aware generation. Skipping intent classification and passing every email directly to an LLM for reply generation is expensive and...
- Email thread summarization for long chains (>10 messages) must use a sliding window or map-reduce approach rather than naive full-context summarization, because most LLMs have context windows that cannot reliably handle 50+ message threads, and even...
- Action item extraction from emails requires distinguishing between three types of obligations that are easy to conflate: (1) items the recipient must do ('Please send the contract by Friday'), (2) items the sender will do ('I will follow up with the...

## RATIONALE (why it works)
- Email classification must be treated as a multi-label problem, not a single-label one, because real emails routinely belong to multiple categories simultaneously — a message can be both a meeting request and a follow-up on an invoice. Systems that...
- OAuth2 for Gmail and Outlook is not merely an authentication upgrade from password-based access — it is the only operationally viable approach because application passwords are being deprecated across enterprise tenants (Google Workspace enforces...
- Attachment handling in email automation pipelines requires explicit MIME type validation against file content (magic bytes), not just file extension checking. Extensions are trivially spoofed: a malicious script renamed to .pdf will pass...
