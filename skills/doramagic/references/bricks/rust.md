# rust — 知识摘要

## CRITICAL (failure)
- FAILURE: Lifetime errors confuse people because the compiler reports where borrowing becomes impossible, not where ownership design first went wrong. The hidden lesson is that most lifetime pain is a data-shape problem: returning owned values, narrowing...
- FAILURE: Async runtime choice is a platform decision, not a crate preference. Tokio offers the deepest ecosystem and operational tooling; lighter runtimes reduce surface area but make library interop and examples scarcer. The hidden failure is deferring the...
- FAILURE: Arc<Mutex<T>> is the safe default for shared mutable state across threads, but it becomes a correctness trap when Mutex guards are held across await points in async code. Tokio's async runtime cannot make progress on a task that holds a blocking...
- FAILURE: Rust's borrow checker enforces exclusivity: you cannot have a mutable reference while any immutable reference exists. The hidden pain point emerges in struct methods when you need to mutate one field while reading another. The borrow checker sees...
- FAILURE: Rust's Send and Sync auto-traits are silently removed by wrapping types in Rc, RefCell, or raw pointers. The failure is invisible until code crosses a thread boundary: the compiler then emits an error pointing at the async block or spawn call, not...
- FAILURE: String and &str confusion is Rust's most common beginner failure but also a persistent expert trap in async contexts. Holding a &str reference across an await point fails to compile if the referenced data might be dropped before the future resumes....

## CONSTRAINTS
- Unsafe is justified only when it contains an invariant the type system cannot express yet. The hidden rule is to move unsafety to the smallest possible module, document the invariant in plain language, and present a safe API outside it. If callers...
- Pin and Unpin matter when movement itself can break correctness, especially for self-referential async state machines. The hidden mistake is treating Pin as an async tax you can ignore. If an API mentions Pin, it is telling you that address...

## PATTERNS (how to build)
- Rust error handling works best as a layered contract. Option means absence is expected, Result means a caller can act, thiserror defines stable library errors, and anyhow belongs at application boundaries where context matters more than...
- Cargo workspaces are more than a mono-repo convenience. They let teams separate domain crates, binaries, and internal tooling while sharing lockfiles and build logic. The hidden benefit is sociotechnical: crate boundaries force cleaner APIs and make...
- Tokio's select! macro enables concurrent waiting on multiple futures, but each arm must be cancel-safe: when select! drops a future that was not selected, any intermediate state it accumulated is lost. The hidden pattern is that futures doing...

## RATIONALE (why it works)
- Ownership and borrowing are not only memory rules; they are Rust's way of forcing API authors to state aliasing and lifetime intent. The hidden benefit is architectural honesty: data flow, mutation boundaries, and thread transfer become visible in...
- Zero-cost abstractions in Rust mean you are expected to write the clear abstraction first and inspect performance only when needed. The unspoken contract is that iterators, enums, and generics are not high-level compromises; they are the normal...
