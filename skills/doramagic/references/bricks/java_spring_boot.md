# java_spring_boot — 知识摘要

## CRITICAL (failure)
- FAILURE: Circular dependency injection (A depends on B, B depends on A) causes BeanCurrentlyInCreationException at startup with constructor injection — Spring cannot satisfy both at the same time. With field injection (@Autowired), Spring historically...
- FAILURE: @Transactional works through Spring AOP proxies: when you inject a @Service bean, you get a proxy, and calling a @Transactional method through the proxy triggers transaction management. But if a method inside the same class calls another...
- FAILURE: The JPA N+1 query problem: if you fetch a list of 100 Order entities and each Order has a lazy-loaded List<OrderItem>, accessing orderItem fields in a loop triggers 100 additional SELECT queries (1 for orders + N for items). This is silent — no...
- FAILURE: Spring WebFlux reactive pipelines silently block the event loop when code calls a blocking method—JDBC, Files.readAllBytes, Thread.sleep—inside a Mono/Flux operator. The event loop has a fixed small thread pool, so one blocking call stalls all...
- FAILURE: Spring Security's CSRF protection is enabled by default for stateful (session-based) apps but disabled by default for stateless (JWT/API) apps when using the lambda DSL. The hidden failure is that teams extending the wrong SecurityFilterChain base...
- FAILURE: JPA's @OneToOne with fetch = LAZY is silently treated as EAGER when the association is on the owning side without a @LazyToOne hint. Hibernate cannot make a lazy proxy for the owning side of a @OneToOne because it must check whether the associated...

## CONSTRAINTS
- Spring Boot's default connection pool (HikariCP) has a default pool size of 10 connections. Under moderate load, a service making synchronous database calls from a thread-per-request model saturates the pool and queues threads waiting for...
- Spring Data JPA's `@OneToMany(fetch = FetchType.EAGER)` on a collection causes N+1 queries by default when used with `findAll()`. The Hibernate query for a list of 100 entities with two eager collections issues 1 + 100 + 100 = 201 queries. The...

## PATTERNS (how to build)
- @SpringBootTest starts the full application context including all beans, auto-configuration, and embedded server, making it the most realistic but slowest test type. The correct layered testing strategy is: @WebMvcTest for controller layer only...
- GraalVM Native Image compilation for Spring Boot 3 requires all reflective access, proxy generation, and resource loading to be declared up front via @RegisterReflectionForBinding, @ImportRuntimeHints, or native-image.properties. The hidden payoff...
- Spring Batch is the correct framework for large-scale ETL in Spring Boot applications because it provides chunk-oriented processing, restartability from the last successful checkpoint, and job execution metadata out of the box. The hidden pattern is...

## RATIONALE (why it works)
- Spring Boot's core value proposition is convention over configuration: the framework makes opinionated default decisions so developers write zero XML and minimal properties files for standard use cases. If spring-data-jpa is on the classpath and a...
- Spring's Inversion of Control (IoC) container is its architectural foundation: instead of objects creating their own dependencies (new MyService()), the container creates and wires everything. Dependency Injection is the mechanism — objects declare...
- Spring Boot auto-configuration works via the @EnableAutoConfiguration mechanism: at startup, Spring Boot reads META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports (Spring Boot 3.x) or META-INF/spring.factories (Spring...
