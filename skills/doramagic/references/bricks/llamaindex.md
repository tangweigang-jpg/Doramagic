# llamaindex — 知识摘要

## CRITICAL (failure)
- FAILURE: The v0.10 package rename from llama_index to llama-index-core broke a massive base of tutorials, blog posts, and Stack Overflow answers. Pre-v0.10 code uses `from llama_index import ...` or `import llama_index`; post-v0.10 the canonical...
- FAILURE: LlamaIndex's default chunk_size of 1024 tokens and chunk_overlap of 20 tokens are rarely optimal and produce poor retrieval quality on most real-world corpora without explicit tuning. The 20-token overlap is too small to preserve semantic...
- FAILURE: LlamaIndex's metadata filtering API (MetadataFilter, MetadataFilters) is a portability abstraction over per-vector-store filter syntax, but implementation completeness varies significantly. Pinecone supports all filter operators; Chroma supports a...
- FAILURE: LlamaIndex query engines and retrievers expose both sync and async APIs (query() vs aquery(), retrieve() vs aretrieve()), but many built-in retrievers and response synthesizers only implement the sync path — the async methods fall back to running...
- FAILURE: LlamaIndex's default chunk_size for SentenceSplitter is 1024 tokens, but the default chunk_overlap is 200 tokens, meaning consecutive chunks share 200 tokens of context. For OpenAI embedding models with a 8192-token limit, this overlap is...
- FAILURE: LlamaIndex's RetrieverQueryEngine uses similarity_top_k=2 by default, returning only 2 chunks for any query regardless of document count or query complexity. For knowledge bases with many relevant documents, this severely limits recall: the correct...

## CONSTRAINTS
- LlamaIndex ships over 300 integration packages on PyPI (llama-index-llms-openai, llama-index-embeddings-huggingface, llama-index-vector-stores-chroma, etc.). pip install llama-index installs llama-index-core plus a minimal default set, but does NOT...
- The choice of embedding model silently determines retrieval quality in ways LlamaIndex does not surface at runtime. The default embedding model (text-embedding-ada-002, 1536 dimensions) is set globally via Settings.embed_model; if a user loads a...

## PATTERNS (how to build)
- The recommended LlamaIndex pattern for production RAG evaluation is to use Ragas or TruLens for automated quality measurement across three dimensions: faithfulness (does the answer stay grounded in retrieved context?), answer relevancy (does the...
- The recommended LlamaIndex pattern for real-time document updates (when the source corpus changes frequently) is a differential ingestion pipeline: track document modification timestamps, re-ingest only modified documents, and use docstore...
- The recommended LlamaIndex pattern for multi-hop reasoning (questions requiring chaining 2+ retrieval steps) is agent-based query decomposition using ReActAgent or FunctionCallingAgent with multiple QueryEngineTools. Each tool represents a different...

## RATIONALE (why it works)
- LlamaIndex is a data framework for LLM applications that treats indexing as a first-class citizen: rather than bolting retrieval onto an LLM call, it structures the entire pipeline around ingestion, index construction, and query-time retrieval. This...
- VectorStoreIndex, KnowledgeGraphIndex, and PropertyGraphIndex serve fundamentally different retrieval semantics: VectorStoreIndex embeds each Node and retrieves by cosine similarity — best for open-ended semantic search over unstructured text....
- LlamaIndex QueryEngine uses response synthesis modes that trade quality for cost and latency. `refine` mode sends each retrieved chunk to the LLM sequentially, asking it to refine the answer — highest quality but O(k) LLM calls. `compact` mode...
