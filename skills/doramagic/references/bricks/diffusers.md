# diffusers — 知识摘要

## CRITICAL (failure)
- FAILURE: Loading a full SDXL pipeline naively allocates ~6.5 GB VRAM for FP16 weights (UNet ~2.6 GB, VAE ~160 MB, two CLIP text encoders ~1.4 GB each). FLUX.1 dev requires ~24 GB in FP16. The failure mode is calling from_pretrained() without memory...
- FAILURE: Not all schedulers are compatible with all models, and Diffusers does not always raise an informative error when an incompatible scheduler is used. Specific failure patterns: (1) Flow-matching models (FLUX, SD3) require schedulers that implement the...
- FAILURE: Diffusers' VAE decode step is a significant source of memory and time overhead that many tutorials underemphasize. For SDXL, decoding a single 1024x1024 image requires the full VAE decoder with ~160MB of parameters — but the intermediate feature...
- FAILURE: Diffusers' inpainting pipelines (StableDiffusionInpaintPipeline, StableDiffusionXLInpaintPipeline) require the mask to be a PIL Image or tensor with values strictly in {0, 1} — 0 meaning 'preserve this region', 1 meaning 'inpaint this region'. A...
- FAILURE: Diffusers' pipeline.to(dtype=torch.float16) applies FP16 to all pipeline components uniformly, but the VAE encoder and decoder are numerically unstable in FP16 for certain image distributions — producing black images, NaN artifacts, or extreme...
- FAILURE: Diffusers' model loading from local directories fails silently in subtle ways when the directory structure deviates from the expected HuggingFace Hub format. The most common failure: loading a Stable Diffusion checkpoint where the unet, vae, and...

## CONSTRAINTS
- Diffusers' seed handling for reproducible generation requires setting the generator object correctly. The common mistake is calling `torch.manual_seed(seed)` globally and expecting deterministic outputs — this works for single-GPU inference but...
- Diffusers' image-to-image pipelines (StableDiffusionImg2ImgPipeline) use the strength parameter (0.0-1.0) to control how much the original image is preserved. The constraint most developers miss: strength determines what fraction of the total...

## PATTERNS (how to build)
- ControlNet conditioning in Diffusers requires the control image to be preprocessed with the appropriate annotator before passing to the pipeline. Each ControlNet variant (canny, depth, pose, seg) expects a specific input format: Canny expects a...
- Diffusers' pipeline callbacks (callback_on_step_end) enable intermediate step inspection and dynamic parameter modification during denoising. The recommended use cases: early stopping when quality metrics stabilize, adaptive guidance scale reduction...
- Diffusers' inpainting pipeline (StableDiffusionInpaintPipeline) requires both an image and a binary mask where white pixels (255) indicate regions to regenerate and black pixels (0) indicate regions to preserve. The mask must be the same spatial...

## RATIONALE (why it works)
- Hugging Face Diffusers organizes all generation workflows around the Pipeline abstraction: a single object that encapsulates a UNet/transformer backbone, a VAE, a text encoder, a scheduler, and optional adapters. The design intent is a stable API...
- In Diffusers the scheduler is not a minor hyperparameter — it is the core algorithmic primitive that defines the denoising trajectory. The scheduler implements the noise schedule (beta values), the timestep sequence, and the denoising step update...
- Diffusers provides a unified LoRA API via pipeline.load_lora_weights() and pipeline.fuse_lora(). The key distinction between loading and fusing: load_lora_weights() applies LoRA as a live adapter (weights stored separately, mergeable at runtime);...
