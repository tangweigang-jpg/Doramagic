# go_general — 知识摘要

## CRITICAL (failure)
- FAILURE: Go maps are NOT safe for concurrent use — concurrent reads with any write cause a data race and panic in Go's race detector. Fix options: (1) sync.RWMutex protecting the map (reads use RLock, writes use Lock), (2) sync.Map (built-in, optimized for...
- FAILURE: Go goroutine leaks are silent memory leaks — a goroutine blocked forever holds its stack (2KB minimum, can grow). Common leak patterns: (1) goroutine waiting on a channel that no sender/receiver will ever serve, (2) goroutine blocked on an HTTP...
- FAILURE: Go `goroutine leak` via WaitGroup misuse: `var wg sync.WaitGroup; for item := range items { wg.Add(1); go func() { defer wg.Done(); process(item) }() }; wg.Wait()`. Before Go 1.22: `item` is captured by reference — all goroutines see the LAST value...
- FAILURE: Go `nil pointer dereference` from interface values: `var err error = (*os.PathError)(nil)` — `err != nil` is TRUE (the interface has a non-nil type). Calling `err.Error()` panics because the underlying value is nil. Common failure: returning a typed...
- FAILURE: Go `map` nil vs empty: `var m map[string]int` — nil map. Reading from nil map: `m["key"]` returns 0 (zero value) — safe. Writing to nil map: `m["key"] = 1` — PANICS with 'assignment to entry in nil map'. Fix: initialize with `m =...
- FAILURE: Go `string` and `[]byte` conversion cost: `string(bytes)` and `[]byte(str)` always allocate and copy. In hot paths with frequent conversions: use `unsafe.String` and `unsafe.SliceData` (Go 1.20+) for zero-copy conversion — safe only when the data...

## CONSTRAINTS
- Go slices share underlying arrays — a slice is a header (pointer, length, capacity). `copy()` creates a deep copy; assignment or slicing creates a view. UNSAID: appending to a sub-slice can overwrite elements of the parent slice if capacity allows:...
- Go `goroutine` stack and memory constraints: goroutines start with a 2KB-8KB stack (Go runtime manages growth via segmented/contiguous stacks). Maximum goroutine stack defaults to 1GB (configurable via `debug.SetMaxStack`). Each goroutine has...

## PATTERNS (how to build)
- Go interfaces are implicitly satisfied. Best practice: define interfaces at the point of USE, not at the point of IMPLEMENTATION. A package that returns a concrete type is more flexible than one that returns an interface — consumers can define...
- Go table-driven tests are the idiomatic testing pattern. Define a slice of test cases (structs with input and expected output), then iterate with t.Run() for each subtest. Benefits: (1) new cases are one-line struct additions, (2) t.Run creates a...
- Go error wrapping with `fmt.Errorf("operation failed: %w", err)` creates an error chain inspectable with errors.Is() and errors.As(). errors.Is() checks if any error in the chain matches a target (by value or Unwrap). errors.As() checks if any error...

## RATIONALE (why it works)
- Go's design philosophy: simplicity, readability, explicit error handling, and built-in concurrency. Go has no generics-based DI framework by design — dependency injection is done manually via constructors. Interfaces are implicit (structural typing,...
- Go returns errors as the last return value (err error). Errors are values — they're compared, wrapped, and inspected with errors.Is() and errors.As(). fmt.Errorf with %w verb wraps errors for unwrapping. UNSAID: panic() is for programming errors...
- Go built-in `min` and `max` functions (1.21+) work on any ordered type — `min(a, b)`, `max(a, b)`, `min(a, b, c, d...)` with variadic support. Before 1.21, Go had no generic min/max; every codebase defined its own `func min(a, b int) int` which...
