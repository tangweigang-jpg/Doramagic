# php_laravel — 知识摘要

## CRITICAL (failure)
- FAILURE: Eloquent N+1 is usually a boundary design failure, not just a missing with call. When templates and serializers pull relations ad hoc, query planning happens too late. The durable fix is to decide data shape at the application boundary and load that...
- FAILURE: Queue workers leak memory when code assumes request-lifetime cleanup still exists in a long-running process. Static caches, large collections, and native resources accumulate quietly because the worker keeps serving jobs. The hidden rule is to code...
- FAILURE: Eloquent model events (creating, updating, deleting) do not fire when using bulk operations like Model::insert(), update(), or delete() on the query builder. The hidden failure is that observers and event listeners registered to enforce business...
- FAILURE: Laravel's job batching silently swallows failed jobs unless a catch callback is registered on the batch. The default behavior marks a job as failed but the batch continues, and the batch's finished callback fires even when some jobs errored. Teams...
- FAILURE: PHP's sessions and static properties persist across requests in Laravel Octane's long-running worker model, but they do not in traditional PHP-FPM. The hidden failure is porting a Laravel app to Octane without auditing for request-scope assumptions:...
- FAILURE: Laravel's Horizon dashboard shows queue metrics based on Redis keys that expire. When Horizon is stopped for an extended period and restarted, historical throughput charts reset and peak queue depth records are lost. The deeper failure is teams...

## CONSTRAINTS
- Laravel's scheduler runs all tasks in a single PHP process, sequentially by default. If a scheduled command runs longer than the schedule interval, the next invocation can overlap or be missed depending on the onOneServer and withoutOverlapping...
- Laravel's database migrations must be backward-compatible with the previous version of the application code during deployments. Adding a NOT NULL column without a default value breaks queries from web workers still running the old code that do not...

## PATTERNS (how to build)
- The service container is Laravel's real extension surface. Its hidden value is not IoC as an academic pattern, but the ability to swap infrastructure, decorate behavior, and keep controllers thin without manual factories everywhere. Once teams...
- Migrations and seeders work best as repeatable environment assembly, not throwaway setup scripts. The hidden pattern is to keep schema evolution deterministic and keep data seeding idempotent or clearly scoped by environment. That discipline makes...
- Laravel middleware is a pipeline architecture disguised as HTTP sugar. The hidden leverage is that cross-cutting rules stay ordered, composable, and request-scoped. Once business logic starts hiding in controllers because a middleware felt too...

## RATIONALE (why it works)
- Laravel's elegant syntax is a product decision: it makes common web work read like intention instead of framework ceremony. The hidden tradeoff is that convenience APIs assume you accept the framework's happy path. Teams get the most leverage when...
- Livewire versus Inertia is less about full stack versus SPA and more about where state authority lives. Livewire favors server-owned UI with PHP-centric teams; Inertia favors client-owned interaction with modern frontend tooling. The hidden mistake...
- Laravel's Eloquent ORM lazy loads relationships by default, producing the N+1 query problem when iterating a collection and accessing related models. The rationale for using eager loading (with(), withCount()) rather than lazy loading in production...
